---
phase: 05-evidence-findings-and-gate-p3
plan: "05"
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-04"]
files_modified:
  - frontend/src/components/evidence/EvidenceDetailPage.tsx
  - frontend/src/components/evidence/EvidenceMetadataBlock.tsx
  - frontend/src/components/evidence/EvidenceFileList.tsx
  - frontend/src/components/evidence/LinkedObjectivesList.tsx
  - frontend/src/components/evidence/LinkObjectivePopover.tsx
  - frontend/src/components/evidence/GapViewPanel.tsx
  - frontend/src/components/evidence/GapObjectiveCard.tsx
  - frontend/src/components/evidence/DeleteEvidenceButton.tsx
  - e2e/evidence-detail.spec.ts

autonomous: true

features:
  implements: ["F8", "F9"]
  depends_on: ["F8", "F9"]
  enables: ["F10"]

must_haves:
  truths:
    - "Evidence Detail page shows metadata (source, custodian, date, description), sensitivity badge, attached files with download links, linked objectives, linked findings"
    - "User can link evidence to additional objectives via LinkObjectivePopover (searchable Command + Popover)"
    - "Delete Evidence is disabled (aria-disabled + tooltip) when evidence is linked; shows AlertDialog when unlinked"
    - "Gap view panel shows objectives with 0 linked evidence as red dashed cards with P3 blocker label and [Link →] button"
    - "Clicking [↓ Download] fetches the file from backend and streams download"
    - "Restricted evidence detail returns ForbiddenPage for AL/RO users"
  artifacts:
    - path: "frontend/src/components/evidence/EvidenceDetailPage.tsx"
      provides: "Full evidence item detail page with metadata, files, linked objectives, linked findings"
      exports: ["EvidenceDetailPage"]
    - path: "frontend/src/components/evidence/LinkObjectivePopover.tsx"
      provides: "Searchable objective selector to link evidence to objective"
      exports: ["LinkObjectivePopover"]
    - path: "frontend/src/components/evidence/GapViewPanel.tsx"
      provides: "Collapsible gap view showing objectives with no linked evidence"
      exports: ["GapViewPanel"]
    - path: "e2e/evidence-detail.spec.ts"
      provides: "Playwright E2E tests for Evidence Detail + Gap View"
  key_links:
    - from: "frontend/src/components/evidence/EvidenceDetailPage.tsx"
      to: "/api/engagements/:id/evidence/:evidence_id"
      via: "useEffect fetch on mount via api.get"
      pattern: "api\\.get.*evidence.*evidence_id"
    - from: "frontend/src/components/evidence/LinkObjectivePopover.tsx"
      to: "/api/engagements/:id/evidence/:evidence_id/objectives"
      via: "POST on objective selection"
      pattern: "api\\.post.*objectives"
    - from: "frontend/src/components/evidence/GapViewPanel.tsx"
      to: "/api/engagements/:id/objectives/coverage"
      via: "useEvidenceCoverage hook; gap view reads coverage.objectives where evidence_count=0"
      pattern: "useEvidenceCoverage|coverage\\.objectives"

integration_contracts:
  requires:
    - from_plan: "05-01"
      artifact: "backend/src/routes/evidence.ts"
      exports: ["GET /api/engagements/:id/evidence/:evidence_id", "DELETE /api/engagements/:id/evidence/:evidence_id", "GET /api/engagements/:id/evidence/:evidence_id/files/:file_id/download"]
      verify: "grep -n 'evidenceRouter' backend/src/routes/evidence.ts && echo CONTRACT_OK"
    - from_plan: "05-02"
      artifact: "backend/src/routes/objectivecoverage.ts"
      exports: ["POST /api/engagements/:id/evidence/:evidence_id/objectives", "DELETE /api/engagements/:id/evidence/:evidence_id/objectives/:objective_id", "GET /api/engagements/:id/objectives/coverage"]
      verify: "grep -n 'objectives/coverage\|objectives/sufficiency' backend/src/routes/objectivecoverage.ts && echo CONTRACT_OK"
    - from_plan: "05-04"
      artifact: "frontend/src/components/evidence/SensitivityBadge.tsx"
      exports: ["SensitivityBadge"]
      verify: "grep -n 'export.*SensitivityBadge' frontend/src/components/evidence/SensitivityBadge.tsx && echo CONTRACT_OK"
  provides:
    - artifact: "frontend/src/components/evidence/GapViewPanel.tsx"
      exports: ["GapViewPanel"]
      shape: |
        export const GapViewPanel: React.FC<{
          engagementId: string;
          objectives: CoverageObjective[];
          onLinkClick: (objective: CoverageObjective) => void;
        }> = ({ engagementId, objectives, onLinkClick }) => { ... }
        // Renders red dashed cards per objective with evidence_count=0
      verify: "grep -n 'export.*GapViewPanel' frontend/src/components/evidence/GapViewPanel.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/components/evidence/LinkObjectivePopover.tsx"
      exports: ["LinkObjectivePopover"]
      shape: |
        export const LinkObjectivePopover: React.FC<{
          engagementId: string;
          evidenceId: string;
          linkedObjectiveIds: string[];
          onLinked: (objectiveId: string) => void;
        }> = ({ ... }) => { ... }
      verify: "grep -n 'export.*LinkObjectivePopover' frontend/src/components/evidence/LinkObjectivePopover.tsx && echo CONTRACT_OK"
---

<objective>
Implement Evidence Detail page (F8/F9): full metadata display, file download links, objective linking via popover, delete with link guard, and the Gap View panel (objectives with no evidence, red dashed cards with P3 blocker indicator).

Purpose: Evidence Detail is the drill-down for each evidence item. Gap View (also in this plan) completes the evidence coverage picture for analysts. Shares hooks and primitives from Plan 04. Plans 06 and 07 consume GapViewPanel.
Output: EvidenceDetailPage, GapViewPanel, LinkObjectivePopover, DeleteEvidenceButton with Playwright E2E tests.
</objective>

<feature_dependencies>
Implements: F8: Evidence detail page (metadata, files, download, edit, delete with link guard); F9: Objective linking from evidence detail (LinkObjectivePopover), gap view (GapViewPanel — objectives with 0 evidence shown as P3 blockers)
Depends on: F8 backend (Plan 05-01), F9 backend (Plan 05-02), shared primitives from Plan 05-04 (SensitivityBadge, EvidenceTypeBadge, useEvidence, useEvidenceCoverage)
Enables: F10 UI (Plan 06 Findings list and Plan 07 Gate P3 review consume GapViewPanel)
</feature_dependencies>

<execution_context>
@/root/.config/opencode/pivota_spec-framework/workflows/execute-plan.md
@/root/.config/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-evidence-findings-and-gate-p3/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: EvidenceDetailPage — metadata, files, linked objectives, delete guard</name>
  <files>
    frontend/src/components/evidence/EvidenceDetailPage.tsx
    frontend/src/components/evidence/EvidenceMetadataBlock.tsx
    frontend/src/components/evidence/EvidenceFileList.tsx
    frontend/src/components/evidence/LinkedObjectivesList.tsx
    frontend/src/components/evidence/LinkObjectivePopover.tsx
    frontend/src/components/evidence/DeleteEvidenceButton.tsx
  </files>
  <action>
Build the Evidence Detail page per UI-SPEC Section "Screen 3 — Evidence Detail Page".

**`EvidenceMetadataBlock.tsx`:**
Per UI-SPEC Screen 3 metadata section:
- Background: `hsl(210 20% 97%)` (secondary — slate-50/slate-100 tint)
- Border: 1px `hsl(214 32% 91%)`, radius 6px, padding 16px
- Two-column grid at ≥768px (label left, value right); single column mobile
- Columns: SOURCE | CUSTODIAN | DATE RECEIVED | UPLOADED BY | DESCRIPTION
- Label: 12px/400 muted uppercase tracking-wide
- Value: 14px/400 body
- `UPLOADED BY` = `created_by` user name + " · " + formatted `created_at` date

**`EvidenceFileList.tsx`:**
Per UI-SPEC Screen 3 "Attached Files section":
- Each file row: 48px height (touch-target)
- File icon: lucide-react `FileText` (PDF/DOCX), `FileSpreadsheet` (XLSX/CSV), `Image` (PNG/JPG), `File` (others)
- Filename: 14px/400
- Size: 12px/400 muted (formatted: "2.4 MB")
- `[↓ Download]` button (secondary/outline, 12px): `aria-label="Download {filename}"`
  - On click: fetch `GET /api/engagements/:id/evidence/:evidenceId/files/:fileId/download` → create object URL from blob → trigger download
  - Loading: spinner inline
- `[+ Upload additional file]` at bottom: secondary/outline link; triggers file input; respects 20-file max
- Empty state (no files): "No files attached yet." (12px/400 muted inline)

**`LinkedObjectivesList.tsx`:**
Per UI-SPEC Screen 3 "Linked Objectives section":
- Each linked objective: `CheckCircle` icon (16px, green-600) + objective text truncated at 80 chars (14px/400)
- `[+ Link to another objective]` accent link below list; opens `LinkObjectivePopover`
- When no objectives linked: "No objectives linked yet. Link this evidence to objectives to support coverage." (12px/400 muted)
- Already-linked objectives props: `linkedObjectiveIds: string[]`

**`LinkObjectivePopover.tsx`:**
Per UI-SPEC Screen 3 "LinkObjectivePopover":
- Uses shadcn `Popover` + `Command` (searchable list — same pattern as Phase 4 UserSearchCombobox and Phase 2 global search)
- On open: loads all objectives for engagement via `useEvidenceCoverage` data (already cached in parent)
- Each result: objective text truncated at 60 chars + current link count
- Already-linked objectives: shown with `✓` and `aria-disabled="true"` (visually disabled)
- On select (unlinked objective): immediately calls `POST /api/engagements/:id/evidence/:evidenceId/objectives` with `{ objective_ids: [selectedId] }` → toast "Evidence linked to objective." → call `onLinked(objectiveId)`
- On duplicate attempt (already linked): toast (amber warning) "Already linked to this objective."
- Props: `{ engagementId, evidenceId, linkedObjectiveIds, onLinked, allObjectives }`

**`DeleteEvidenceButton.tsx`:**
Per UI-SPEC Screen 3 "Action buttons (bottom)":
- `[Delete Evidence]` ghost/destructive button
- If evidence has `objective_count > 0` OR `finding_count > 0` (tracked via the evidence item response):
  - Button: `aria-disabled="true"` (NOT `disabled`) + `cursor-not-allowed`
  - Tooltip (shadcn Tooltip): "Cannot delete — linked to objectives or findings. Unlink first."
  - `aria-describedby` pointing to tooltip text
- If evidence unlinked: opens `DeleteEvidenceConfirm` AlertDialog
  - AlertDialog max-width 480px
  - Title: "Delete Evidence Item E-{NNN}?"
  - Body: "This will permanently remove the evidence record and all attached files. This action cannot be undone."
  - Buttons: `[Keep Evidence]` (ghost) + `[Delete Evidence]` (destructive red)
  - Loading: spinner + "Deleting..." disabled
  - On confirm: `DELETE /api/engagements/:id/evidence/:evidenceId` → redirect to Evidence list → toast "Evidence item deleted."
  - On 409 from backend: show toast error "Cannot delete — linked to objectives or findings."

**`EvidenceDetailPage.tsx`:**
Per UI-SPEC Screen 3:
- Route: `/engagements/:engagementId/evidence/:evidenceId`
- On mount: fetch `GET /api/engagements/:id/evidence/:evidenceId` (create this specific endpoint in the route — currently Plan 05-01 has list + create; add GET single to evidenceRouter)
  - Actually: use the list endpoint filtered by ID (OR add a GET /:evidence_id endpoint). Prefer a dedicated GET /:evidence_id endpoint. If it doesn't exist yet, implement it by querying from the list or adding the route in a backend patch. Note to executor: check if `GET /api/engagements/:id/evidence/:evidence_id` exists in evidence.ts; if missing, add it to backend/src/routes/evidence.ts and backend/src/services/evidence.service.ts with a `getEvidence(engagementId, evidenceId, viewerRoles)` function.
- If fetch returns 403: render `ForbiddenPage` (imported from Phase 2 components)
- Page header: `SensitivityBadge` (from Plan 04) + evidence type label (14px/400 muted) + date received (14px/400 muted)
- Breadcrumb: "Engagements > ENG-... > Evidence > E-{NNN}"
- Layout sections: metadata block | linked objectives | attached files | linked findings | action buttons
- Action buttons: `[Edit Evidence]` (opens AddEvidencePanel pre-populated, AN/AD only) + `[Delete Evidence]` (DeleteEvidenceButton) + `[View Audit Trail →]` link
- `[Edit Evidence]` passes existing evidence data as defaultValues to AddEvidencePanel; on save: PATCH endpoint; toast "Evidence item updated."
- Linked Findings section: list `finding_id` refs (query from findings list filtered to this evidence — or use a relationship field if available in evidence response)
  </action>
  <verify>
grep -n 'export.*EvidenceDetailPage' frontend/src/components/evidence/EvidenceDetailPage.tsx && echo "DETAIL_PAGE_OK"
grep -n 'export.*EvidenceMetadataBlock' frontend/src/components/evidence/EvidenceMetadataBlock.tsx && echo "METADATA_OK"
grep -n 'export.*EvidenceFileList' frontend/src/components/evidence/EvidenceFileList.tsx && echo "FILE_LIST_OK"
grep -n 'export.*LinkedObjectivesList' frontend/src/components/evidence/LinkedObjectivesList.tsx && echo "LINKED_OBJ_OK"
grep -n 'export.*LinkObjectivePopover' frontend/src/components/evidence/LinkObjectivePopover.tsx && echo CONTRACT_OK
grep -n 'export.*DeleteEvidenceButton' frontend/src/components/evidence/DeleteEvidenceButton.tsx && echo "DELETE_BTN_OK"
grep -n 'aria-disabled\|TooltipContent\|Cannot delete' frontend/src/components/evidence/DeleteEvidenceButton.tsx && echo "DELETE_GUARD_OK"
grep -n 'Command\|Popover\|searchable' frontend/src/components/evidence/LinkObjectivePopover.tsx && echo "LINK_POPOVER_OK"
grep -n 'ForbiddenPage\|403\|forbidden' frontend/src/components/evidence/EvidenceDetailPage.tsx && echo "FORBIDDEN_OK"
  </verify>
  <done>
EvidenceDetailPage renders metadata block (2-column at ≥768px), file list with download buttons, linked objectives with check icons, linked findings, and action buttons. LinkObjectivePopover uses Command+Popover for searchable objective linking. DeleteEvidenceButton shows tooltip when linked, AlertDialog when unlinked. Restricted evidence detail redirects AL/RO to ForbiddenPage.
  </done>
</task>

<task type="auto">
  <name>Task 2: GapViewPanel + Playwright E2E tests (evidence detail + gap view)</name>
  <files>
    frontend/src/components/evidence/GapViewPanel.tsx
    frontend/src/components/evidence/GapObjectiveCard.tsx
    e2e/evidence-detail.spec.ts
  </files>
  <action>
Build the Gap View panel and write Playwright E2E tests for Evidence Detail and Gap View.

**`GapObjectiveCard.tsx`:**
Per UI-SPEC Section "Screen 4 — Gap View":
- Background: `hsl(0 72% 93%)` (red-100)
- Border: `2px dashed hsl(0 72% 51%)` (red-600)
- Border-radius: 8px; padding: 16px
- `role="article"` + `aria-label="Gap: Objective {N} — no linked evidence — P3 blocker"`
- Contents:
  - Objective text: 14px/400, max 2 lines (truncated at 100 chars per FRD, overflow ellipsis)
  - Sufficiency chip: `SufficiencyChip` showing "Evidence Needed" (red-100/red-700 with 1px red-600 border, 24px height)
    - Create inline `SufficiencyChip` component in this file (used by Plan 07 too — export it separately as `frontend/src/components/evidence/SufficiencyChip.tsx`)
  - "🔴 None" evidence badge: red-100 bg, red-700 text, 12px/400
  - "Blocker" P3 indicator: `XCircle` icon (16px, red-600) + "Blocker" text (12px/400 red-700)
  - Days countdown: 12px/400 muted — "N days until evidence readiness milestone" — compute from engagement milestones if available; show "Past milestone" if date in past; show nothing if milestone date not set
  - `[Link →]` button: accent-colored ghost link → opens `LinkObjectivePopover` pre-filtered to this objective

**`GapViewPanel.tsx`:**
Per UI-SPEC Screen 4:
- Container title: "Evidence Gap View — Objectives with No Evidence"
- Summary text: "N of M objectives have no linked evidence (P3 blocker)"
- Renders one `GapObjectiveCard` per objective where `evidence_count = 0`
- When all objectives have evidence:
  - Background green-100, text green-800: "All objectives have linked evidence. No gaps."
  - Hide toggle button entirely
- Props:
  ```typescript
  type Props = {
    engagementId: string;
    objectives: CoverageObjective[];  // from useEvidenceCoverage
    onLinkClick: (objective: CoverageObjective) => void;
    milestones?: { evidence_readiness_target?: string };
  };
  ```
- Export both `GapViewPanel` and `GapObjectiveCard`

**Create `frontend/src/components/evidence/SufficiencyChip.tsx`** (shared across Plans 05, 06, 07):
Per UI-SPEC "Phase 5 additional semantic colors — P3 Sufficiency Status chips":
- Evidence Needed: bg `hsl(0 72% 93%)` red-100, text `hsl(0 72% 38%)` red-700, border `1px solid hsl(0 72% 51%)` red-600
- In Review: bg `hsl(45 96% 88%)` yellow-100, text `hsl(45 90% 28%)` yellow-800, border `1px solid hsl(45 90% 52%)` yellow-500
- Sufficient: bg `hsl(142 71% 88%)` green-100, text `hsl(142 70% 28%)` green-800, border `1px solid hsl(142 71% 45%)` emerald-600
- Height: 24px; inline-flex; rounded 4px; padding 2px 8px; 12px/400 label role
- Icons: `XCircle` 16px red-600 (Evidence Needed), `Clock` 16px yellow-500 (In Review), `CheckCircle` 16px green-600 (Sufficient)
- 1px colored border distinguishes from Phase 4 milestone chips which have no border

```typescript
export type SufficiencyStatus = 'evidence_needed' | 'in_review' | 'sufficient';
export const SUFFICIENCY_LABELS: Record<SufficiencyStatus, string> = {
  evidence_needed: 'Evidence Needed',
  in_review: 'In Review',
  sufficient: 'Sufficient',
};
export const SufficiencyChip: React.FC<{ status: SufficiencyStatus }> = ({ status }) => { ... };
```

**`e2e/evidence-detail.spec.ts`:**
Write Playwright tests:
1. `evidence detail page loads` — navigate to evidence detail route, expect sensitivity badge and metadata block visible
2. `restricted evidence redirects AL user to forbidden` — login as AL user, navigate to restricted evidence detail URL, expect ForbiddenPage or 403 content
3. `delete evidence button disabled when linked` — mock evidence with objective_count > 0, expect Delete Evidence button has aria-disabled="true" and tooltip visible on hover
4. `delete evidence shows alert dialog when unlinked` — mock evidence with objective_count=0, click Delete Evidence, expect AlertDialog with "Delete Evidence Item" heading
5. `link objective popover opens and links` — click "+ Link to another objective", expect Command popover opens, select an objective, expect toast "Evidence linked to objective."
6. `gap view shows red dashed cards for uncovered objectives` — mock coverage with uncovered objectives, expand gap view, expect red dashed card with XCircle Blocker indicator
7. `file download triggers browser download` — click [↓ Download], mock API response, expect download initiated

Use `page.route()` to mock API calls. Follow existing Playwright patterns.
  </action>
  <verify>
grep -n 'export.*GapViewPanel' frontend/src/components/evidence/GapViewPanel.tsx && echo CONTRACT_OK
grep -n 'export.*GapObjectiveCard' frontend/src/components/evidence/GapObjectiveCard.tsx && echo "GAP_CARD_OK"
grep -n 'export.*SufficiencyChip\|export.*SufficiencyStatus' frontend/src/components/evidence/SufficiencyChip.tsx && echo "SUFFICIENCY_CHIP_OK"
ls frontend/src/components/evidence/SufficiencyChip.tsx && echo "CHIP_FILE_OK"
grep -n 'role.*article\|P3 blocker' frontend/src/components/evidence/GapObjectiveCard.tsx && echo "GAP_A11Y_OK"
grep -n 'red-100\|dashed\|hsl(0 72' frontend/src/components/evidence/GapObjectiveCard.tsx && echo "GAP_COLORS_OK"
grep -n 'test\|expect' e2e/evidence-detail.spec.ts && echo "E2E_EXISTS"
npx playwright test e2e/evidence-detail.spec.ts --reporter=list 2>&1 | tail -30 && echo "PLAYWRIGHT PASSED"
  </verify>
  <done>
GapObjectiveCard renders red-100 bg + 2px dashed red-600 border + role="article" + objective text truncated at 100 chars + SufficiencyChip "Evidence Needed" + Blocker indicator + [Link →] button. GapViewPanel filters coverage objectives where evidence_count=0. SufficiencyChip exported as shared primitive with 3 status variants (1px border distinguishes from Phase 4 chips). All 7 Playwright tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
# 1. SufficiencyChip exported as shared component
grep 'export.*SufficiencyChip' frontend/src/components/evidence/SufficiencyChip.tsx && echo PASS

# 2. GapObjectiveCard uses correct colors
grep 'dashed\|red-100\|hsl(0 72' frontend/src/components/evidence/GapObjectiveCard.tsx && echo PASS

# 3. DeleteEvidenceButton aria-disabled when linked
grep 'aria-disabled' frontend/src/components/evidence/DeleteEvidenceButton.tsx && echo PASS

# 4. EvidenceDetailPage handles 403 for restricted
grep 'ForbiddenPage\|403\|forbidden' frontend/src/components/evidence/EvidenceDetailPage.tsx && echo PASS

# 5. Playwright tests pass
npx playwright test e2e/evidence-detail.spec.ts --reporter=list 2>&1 | tail -5

# 6. Frontend builds
cd frontend && npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- EvidenceDetailPage renders for AN user: SensitivityBadge in header + 5-field metadata block + file list + linked objectives + linked findings + 3 action buttons
- EvidenceDetailPage returns ForbiddenPage for AL user accessing restricted evidence
- DeleteEvidenceButton: aria-disabled + tooltip when objective_count > 0; AlertDialog when unlinked
- GapViewPanel shows red dashed cards for objectives with evidence_count=0; green all-covered message when all covered
- SufficiencyChip renders Evidence Needed (red, 1px border), In Review (amber, 1px border), Sufficient (green, 1px border)
- All 7 Playwright tests pass (0 failing, 0 skipped)
- Tests cover: page load, AL-redirect, delete-disabled, delete-dialog, link-popover, gap-view, file-download
</success_criteria>

<output>
After completion, create `.planning/phases/05-evidence-findings-and-gate-p3/05-05-SUMMARY.md`
</output>
