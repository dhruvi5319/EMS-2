---
phase: 05-evidence-findings-and-gate-p3
plan: "08"
type: execute
wave: 5
depends_on: ["05-07"]
files_modified: []
autonomous: false

features:
  implements: ["F8", "F9", "F10"]
  depends_on: ["F8", "F9", "F10"]
  enables: []

must_haves:
  truths:
    - "Analyst can add evidence, upload files, and view evidence list"
    - "QA can link evidence to objectives and view gap view"
    - "QA can create findings linked to evidence"
    - "QA can approve Gate P3 and engagement advances to draft phase"
  artifacts: []
  key_links: []

integration_contracts:
  requires: []
  provides: []
---

<objective>
Human verification of the complete Phase 5 evidence and findings workflow: evidence registry, file upload, objective linking, gap view, findings, sufficiency management, and Gate P3 approval.

Purpose: Confirm all Phase 5 requirements (F8, F9, F10) are functionally complete via end-to-end manual testing in the running application.
Output: Human sign-off that Phase 5 is production-ready.
</objective>

<feature_dependencies>
Implements: F8: Evidence registry (verified), F9: Evidence-to-objective links + gap view (verified), F10: Findings + sufficiency + Gate P3 (verified)
Depends on: All Phase 5 plans (05-01 through 05-07) complete
Enables: Phase 6 (Draft Product, Reference Check, Gate P4) — Phase 5 must pass before Phase 6 starts
</feature_dependencies>

<execution_context>
@/root/.config/opencode/pivota_spec-framework/workflows/execute-plan.md
@/root/.config/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: complete Phase 5 evidence, findings, and Gate P3 workflow</name>
  <action>Manual end-to-end verification of F8 (evidence registry + file upload + sensitivity), F9 (objective linking + gap view + CSV export), and F10 (findings + sufficiency management + Gate P3 approval).</action>
  <what-built>
Complete Phase 5 implementation:
- Evidence API (F8): evidence CRUD, multi-file upload (StorageProvider), sensitivity-based access (AL/RO excluded from restricted), CSV export
- Objective-Evidence links (F9): link/unlink endpoints, objective coverage summary, gap view data, sufficiency status management
- Findings + Gate P3 (F10): findings CRUD, P3 prerequisites check (4 conditions), P3 gate decision (approve → engagement.phase=draft, GateDecision + AuditEvent)
- Evidence Registry UI: filterable table, coverage bar, Add Evidence Sheet panel, file upload with progress bars
- Evidence Detail UI: metadata, file download, objective linking popover, delete guard
- Gap View: red dashed cards for uncovered objectives with P3 blocker labels
- Findings List UI: FindingCard list, ObjectiveSufficiencySummary bar, P3 prerequisites checklist
- Add Finding Dialog: finding text + evidence multi-select
- Gate P3 Review UI: ObjectiveSufficiencyTable (editable for QA/EM/AD), P3DecisionPanel, ApproveP3ConfirmDialog, ReturnP3ConfirmDialog
- Green success banner on Engagement Shell after P3 approval
  </what-built>
  <how-to-verify>
Start the stack:
```bash
docker compose up -d
# or: cd backend && npm run dev (backend); cd frontend && npm run dev (frontend)
```

**F8 — Evidence Registry:**
1. Login as AN user → navigate to an engagement in `evidence` phase → Evidence tab
2. Verify: coverage bar shows "0 covered · N gap" with red gap segment
3. Click `[+ Add Evidence]` → verify Sheet panel opens from right edge (640px)
4. Fill: Evidence Type=Document, Source="Test Agency", Date=today, Sensitivity=Standard → click `[Save Evidence Item]`
5. Verify: toast "Evidence item saved.", panel closes, evidence appears in list with Standard badge
6. Click `[+ Add Evidence]` again → select Sensitivity=Restricted → save → verify RESTRICTED badge (red-100, lock icon, uppercase) in list
7. Click `[Export CSV ↓]` → verify file downloads; toast "Evidence registry exported."
8. Logout → login as AL user → verify: RESTRICTED row is absent from evidence list DOM (not hidden, absent)

**F9 — Evidence-to-Objective Linking:**
9. Login as AN user → Evidence tab → click `[View →]` on evidence item → Evidence Detail page
10. Verify: metadata block shows Source, Custodian, Date, Description in 2-column layout
11. Click `[+ Link to another objective]` → verify popover opens with searchable objective list
12. Select an objective → verify: toast "Evidence linked to objective.", objective appears in "Linked Objectives" section with ✓ CheckCircle
13. Return to Evidence tab → verify: coverage bar updated (covered count increases)
14. Click `[Show Gaps ▼]` → verify: objectives with 0 evidence show as red dashed cards with "Blocker" label

**F10 — Findings:**
15. Navigate to Findings tab → click `[+ Add Finding]`
16. Verify: Dialog opens with finding text textarea + evidence checkbox list
17. Click Save Finding with empty text → verify: "Finding text is required." error
18. Type finding text + check at least one evidence → click Save Finding → verify: toast "Finding saved.", finding card appears
19. Verify: finding card shows text (3 lines max), evidence chips, "Evidence: [type] chips ✓"
20. Login as QA user → navigate to P3 review page
21. Verify: ObjectiveSufficiencyTable shows all objectives with evidence count + sufficiency Select
22. Set an objective with ≥1 evidence to "Sufficient" → verify: toast "Objective status updated.", chip turns green
23. Attempt to set objective with 0 evidence to "Sufficient" → verify: option disabled with tooltip

**Gate P3:**
24. As QA: ensure all prerequisites pass (link evidence to all objectives, mark all "Sufficient" or "In Review", ensure all findings have evidence)
25. Verify prerequisites checklist shows all ✓ items (no 🔴 blockers)
26. Type decision comment ≥10 chars → `[✓ Approve P3]` becomes active
27. Click `[✓ Approve P3]` → verify: AlertDialog opens with "Approve Evidence Sufficiency (Gate P3)?" title
28. Click `[Confirm Approve P3 ✓]` → verify: toast "Gate P3 approved.", redirect to Engagement Shell
29. Verify: green success banner at top of Engagement Shell ("✅ Gate P3 approved. ENG-... is now in the Draft phase.")
30. Verify: engagement phase now shows "Draft" in shell header
31. Test return flow: return to a new engagement in evidence phase, click `[Return for Revision]` → type comment → `[Confirm Return]` → redirect to Review Queue + toast "P3 review returned for revision."
  </how-to-verify>
  <resume-signal>
Type "approved" once all 31 verification steps pass. If any step fails, describe the issue and which step number failed.
  </resume-signal>
</task>

</tasks>

<verification>
Manual verification performed per 31-step checklist above.
</verification>

<success_criteria>
- Evidence list shows type badges, sensitivity badges (STANDARD gray / RESTRICTED red uppercase), coverage bar
- RESTRICTED rows are entirely absent from AL/RO user's DOM (not hidden with CSS)
- Add Evidence Sheet panel opens, validates required fields, uploads files with progress bars
- CSV export downloads successfully; toast "Evidence registry exported."
- Evidence Detail shows metadata, file download links, linked objectives (CheckCircle), linked findings
- LinkObjectivePopover opens on click; selecting objective links it; toast "Evidence linked to objective."
- Gap view shows red dashed cards for uncovered objectives with P3 blocker indicators
- Add Finding Dialog validates text required; saves finding with evidence links; toast "Finding saved."
- ObjectiveSufficiencyTable: editable for QA/EM/AD; read-only chips for other roles; disabled for 0-evidence objectives
- All 4 P3 prerequisites must pass before [✓ Approve P3] becomes active
- P3 approval: toast "Gate P3 approved." + redirect to Engagement Shell with green success banner
- Engagement.phase updates to "Draft" after approval
- P3 return: redirect to Review Queue + toast "P3 review returned for revision."
</success_criteria>

<output>
After verification, create `.planning/phases/05-evidence-findings-and-gate-p3/05-08-SUMMARY.md`
</output>
