---
phase: 3
slug: intake-and-gate-a1
status: draft
shadcn_initialized: true
preset: manual-init (blue-600 accent, slate-50 secondary, white dominant — Phase 2 token set)
created: 2026-06-05
extends: phases/02-application-shell/02-UI-SPEC.md
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: Intake and Gate A1.
> Generated by pivota_spec-ui-researcher, verified by pivota_spec-ui-checker.
>
> **Phase 3 UI Scope:** Request List page, New/Edit Request Form (F2), Request Detail page,
> Gate A1 Review panel (F3), file upload component, status confirmation states, and empty states.
>
> **Critical constraint:** This spec EXTENDS Phases 1 and 2 — all spacing, typography, color,
> layout constants, and design system tokens are inherited. Nothing in Phase 1 or Phase 2 UI-SPECs
> is contradicted here. Phase 3 adds new form patterns, file upload UX, the Gate A1 decision panel,
> and additional shadcn component contracts.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui — already initialized in Phase 2 | Phase 2 UI-SPEC |
| Preset | Manual init: blue-600 accent, slate-50 secondary, white dominant | Phase 2 color contract |
| Component library | @radix-ui (via shadcn) | Phase 2 UI-SPEC |
| Icon library | lucide-react | Phase 1 UI-SPEC; shadcn standard |
| Font | Inter (system fallback: ui-sans-serif, system-ui) | Phase 1 UI-SPEC |

> **shadcn status:** Already initialized in Phase 2 (Plan 02-01). `components.json` and
> `src/components/ui/` are in place. No re-initialization needed. Phase 3 adds new shadcn
> components via `npx shadcn add {component}`.
>
> **Phase 2 CSS variable tokens (inherited — do not re-declare):**
> - `--background`: `0 0% 100%` · `--card`: `0 0% 100%` · `--secondary`: `210 20% 97%`
> - `--primary`: `221 83% 53%` · `--destructive`: `0 72% 51%`
> - `--muted`: `210 40% 96.1%` · `--muted-foreground`: `215 16% 47%`
> - `--border`: `214 32% 91%` · `--ring`: `221 83% 53%`

---

## Spacing Scale

Inherited from Phase 1 and 2 — no changes. Declared here for executor reference:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline label padding, badge internal padding |
| sm | 8px | Compact element spacing; field label-to-input gap; char counter gap |
| md | 16px | Default element spacing; card internal padding; form field gap |
| lg | 24px | Section padding; card-to-card gap; form section separator spacing |
| xl | 32px | Layout gaps; form container vertical padding |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing; top bar height |

Phase 3 exceptions (new additions — no Phase 1/2 contradictions):
- File upload drop zone min-height: 120px (governs drag-and-drop target area)
- File upload progress bar height: 4px (thin progress indicator)
- A1 decision panel border-left: 4px solid `hsl(221 83% 53%)` (accent — matches active nav bar)
- Character counter positioned 8px (`sm`) below the textarea right edge
- Sticky form actions bar height: 64px (matches top bar height constant)
- Touch target minimum: 44×44px on all interactive controls (inherited from Phase 1/2)

---

## Typography

Inherited from Phase 1 and 2 — no changes. Declared here for executor reference:

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px | 400 (regular) | 1.5 | Form labels, field values, table cells, request detail fields |
| Label | 12px | 400 (regular) | 1.4 | Badge text, status labels, helper text, char counters, error messages, file type hints |
| Heading | 20px | 600 (semibold) | 1.2 | Page titles, section headings, confirmation dialog headings |
| Display | 28px | 600 (semibold) | 1.15 | App wordmark (login page only — not used in Phase 3) |

**Font stack:** `Inter, ui-sans-serif, system-ui, -apple-system, sans-serif`

**Phase 3 typography additions (no contradictions):**
- Form field values after submission (read-only): 14px/400, body color
- Past-due-date warning text: 14px/400, `hsl(45 90% 28%)` (yellow-800) — amber semantic
- Rationale character count indicator: 12px/400, muted → red when below minimum
- Intake document filename in upload zone: 14px/400, body color
- Engagement job code in approval confirmation banner: monospace 14px/400
  (`ui-monospace, 'Cascadia Code', 'Source Code Pro', monospace`)
- Form section labels (e.g., "Request Details", "Intake Document"): 14px/600 — uppercase
  tracking-wide (`letter-spacing: 0.05em`) — sub-section divider treatment

---

## Color

Fully inherited from Phases 1 and 2. No contradictions. Declared here for executor reference:

| Role | Light Mode Value | Usage |
|------|-----------------|-------|
| Dominant (60%) | `hsl(0 0% 100%)` — white | Page backgrounds, content area, form card backgrounds |
| Secondary (30%) | `hsl(210 20% 97%)` — slate-50 | Sidebar, section dividers, read-only field backgrounds |
| Accent (10%) | `hsl(221 83% 53%)` — blue-600 | Reserved list below |
| Destructive | `hsl(0 72% 51%)` — red-600 | Decline button, validation errors, file error messages |
| Muted text | `hsl(215 16% 47%)` — slate-500 | Placeholder text, helper text, secondary field labels, char counters |
| Border | `hsl(214 32% 91%)` — slate-200 | All borders: inputs, cards, dividers, upload zones |
| Surface elevated | `hsl(210 40% 96%)` — slate-100 | Hover states, read-only field backgrounds, drag-over state |

**Accent reserved for (explicit list — no other elements):**

*(Inherits Phase 2 list items 1–9; Phase 3 adds:)*
1. Primary CTA button fill (Submit Request →, Approve Request →)
2. Active nav item left border indicator (4px accent bar)
3. Active nav item text color
4. Focused input/button ring (2px accent ring, 2px offset)
5. Checkbox and toggle checked fill
6. Link text within body content
7. Admin (AD) role badge background
8. Search match highlight background
9. Review Queue badge background
10. **NEW:** A1 decision panel left border accent bar (4px — visual anchoring of gate panel)
11. **NEW:** Approved status banner background tint (`hsl(221 83% 93%)` — blue-100) with accent text
12. **NEW:** "Submit Request" primary CTA button (accent fill, white text)
13. **NEW:** Rationale char count indicator dot (turns accent-colored when ≥10 chars)

**Phase 3 additional semantic colors:**

| Status | Background | Text | Usage |
|--------|-----------|------|-------|
| Request Draft | `hsl(0 0% 93%)` — gray-100 | `hsl(0 0% 35%)` — gray-600 | Request status badge |
| Request Submitted | `hsl(45 96% 88%)` — yellow-100 | `hsl(45 90% 28%)` — yellow-800 | Request status badge |
| Request Accepted | `hsl(142 71% 88%)` — green-100 | `hsl(142 70% 28%)` — green-800 | Request status badge |
| Request Declined | `hsl(0 72% 93%)` — red-100 | `hsl(0 72% 38%)` — red-700 | Request status badge |
| Past due date warning | `hsl(45 96% 88%)` — yellow-100 | `hsl(45 90% 28%)` — yellow-800 | Due date past warning banner |
| File upload idle | `hsl(0 0% 100%)` — white | — | Drop zone background |
| File upload drag-over | `hsl(221 83% 93%)` — blue-100 | — | Drop zone drag-active state |
| File upload error | `hsl(0 72% 93%)` — red-100 | `hsl(0 72% 38%)` — red-700 | Error under drop zone |
| File upload success | `hsl(142 71% 88%)` — green-100 | `hsl(142 70% 28%)` — green-800 | Filename confirmed chip |
| Risk Low | `hsl(142 71% 88%)` — green-100 | `hsl(142 70% 28%)` — green-800 | Risk level badge |
| Risk Medium | `hsl(45 96% 88%)` — yellow-100 | `hsl(45 90% 28%)` — yellow-800 | Risk level badge |
| Risk High | `hsl(0 72% 93%)` — red-100 | `hsl(0 72% 38%)` — red-700 | Risk level badge |

> **Inherited status badge colors from Phase 1/2:** Planning, Evidence, Draft, Readiness,
> Ready for Issuance, Closed phase badges; Gate approved/pending/returned/declined/not-started
> gate card colors — all inherited unchanged.

---

## Component Inventory (Phase 3)

shadcn components to install and use in Phase 3. Run `npx shadcn add {component}` for each new component.

### Form Components (new in Phase 3)

| Component | shadcn Source | Description | States |
|-----------|---------------|-------------|--------|
| `RequestForm` | custom (uses `Form`) | Multi-field request intake form; react-hook-form integration | new (empty), edit (draft), submitting, error |
| `RequestTypeSelect` | `Select` | Request type dropdown: Congressional Request / Mandate / Internal Proposal | idle, selected, error |
| `RequestTextField` | `Input` + `FormField` | Requester, agency/program text inputs | default, focused, error |
| `TopicTextarea` | `Textarea` + `FormField` | Topic field — 500 char max; char counter | default, focused, error, at-limit |
| `NotesTextarea` | `Textarea` + `FormField` | Notes field — 5000 char max; char counter | default, focused, at-limit |
| `DueDatePicker` | `Calendar` + `Popover` | Date picker for due date (MM/DD/YYYY display) | idle, open, selected, past-date-warning |
| `PastDateWarning` | custom | Yellow warning banner: "Due date is in the past..." | hidden, visible |
| `FormSectionHeader` | custom | Section divider: "REQUEST DETAILS" / "INTAKE DOCUMENT" uppercase label | default |
| `FormActions` | custom | Sticky footer bar: [Save as Draft] + [Submit Request →] | default, saving, submitting |

> Run: `npx shadcn add form select textarea calendar popover`

### File Upload (new in Phase 3)

| Component | shadcn Source | Description | States |
|-----------|---------------|-------------|--------|
| `IntakeFileUpload` | custom | Drag-and-drop + click-to-browse; 25MB limit; replaces on re-upload | idle, drag-over, uploading (progress), success, error |
| `FileProgressBar` | `Progress` | 4px thin progress bar during upload | 0–100% |
| `FileConfirmedChip` | custom | Filename + ✓ + × (remove) chip after upload | default, hover (show remove) |
| `FileTypeError` | custom | Red error text below drop zone for type/size errors | hidden, visible |

> Run: `npx shadcn add progress`

### Request List Page (new in Phase 3)

| Component | shadcn Source | Description | States |
|-----------|---------------|-------------|--------|
| `RequestListPage` | custom | Page with status filter tabs + sortable request table | default, filtered, empty |
| `RequestStatusTabs` | `Tabs` | Filter tabs: All / Draft / Submitted / Accepted / Declined | default, active-tab |
| `RequestTable` | `Table` | Sortable request list with columns defined below | default, loading (skeleton), empty |
| `RequestStatusBadge` | custom | Status pill: Draft/Submitted/Accepted/Declined | 4 variants |
| `NewRequestButton` | custom | Primary CTA: "+ New Request" (accent fill) | default, hover |

> Run: `npx shadcn add tabs table`

### Request Detail Page (new in Phase 3)

| Component | shadcn Source | Description | States |
|-----------|---------------|-------------|--------|
| `RequestDetailCard` | custom | Read-only display of all request fields; status badge in header | draft, submitted, accepted, declined |
| `IntakeDocumentCard` | custom | Document download card with file icon and [Download ↓] link | with-file, no-file |
| `RequestAuditLink` | custom | "View Audit Trail →" link (accent) | default |

### Gate A1 Decision Panel (new in Phase 3)

| Component | shadcn Source | Description | States |
|-----------|---------------|-------------|--------|
| `GateA1Panel` | custom | Decision panel visible only when status=submitted AND role=AL | undecided, confirming, decided-read-only |
| `RiskLevelRadioGroup` | `RadioGroup` | 3 radio buttons: Low / Medium / High | unselected, selected-low, selected-medium, selected-high |
| `RationaleTextarea` | `Textarea` | Decision rationale field; char count indicator (≥10 required) | default, focused, too-short (red), valid (green dot) |
| `CharCountIndicator` | custom | "X / 10" minimum chars indicator below rationale | below-min (red), at-min (accent), above-min (muted) |
| `ApproveButton` | custom | Primary action: [✓ Approve Request →] — accent fill | default, disabled (missing inputs), loading |
| `DeclineButton` | custom | Destructive action: [✗ Decline Request] — ghost/outline destructive | default, disabled (missing rationale), loading |
| `ApproveConfirmDialog` | `AlertDialog` | Confirmation: summarizes decision before finalizing | default, confirming, submitting |
| `DeclineConfirmDialog` | `AlertDialog` | Confirmation: "This action cannot be undone." | default, confirming, submitting |
| `GateA1DecidedCard` | custom | Read-only post-decision summary: status, risk, approver, date, rationale | approved-state, declined-state |
| `ApprovalSuccessBanner` | custom | Green banner: "Engagement ENG-YYYY-NNNNN created. [View →]" | visible (after approve), dismissed |

### Shared / Reused from Phase 2 (confirm availability)

| Component | shadcn Source | Already Available | Phase 3 Usage |
|-----------|---------------|-------------------|---------------|
| `Alert` | `Alert` | Add if not present | Validation errors, past-date warning, post-decision banners |
| `Separator` | `Separator` | Add if not present | Form section dividers, A1 panel top border |
| `Badge` | `Badge` | Add if not present | Status badges, risk level badges |
| `PageHeader` | custom | Phase 2 | "Requests", "New Request", "REQ-2026-NNNNN" page headers |
| `Breadcrumb` | shadcn | Phase 2 | "Requests > New Request", "Requests > REQ-2026-NNNNN" |
| `Toast/Toaster` | shadcn | Phase 2 | "Draft saved [time]", "Request submitted successfully.", "Request declined and recorded." |
| `EmptyState` | custom | Phase 2 | Empty request list |
| `SkeletonRow` | shadcn | Phase 2 | Request table loading state |

> Run: `npx shadcn add alert separator badge radio-group`
>
> Full Phase 3 shadcn install command:
> `npx shadcn add form select textarea calendar popover progress tabs table alert separator badge radio-group`

---

## Screen Specifications

### Screen 1: Request List Page

**Source:** UX-Mockup-EMS.md — Information Architecture + Screen-03 (entry)

**Route:** `/requests`

**Access:** AL, EM, RO, AD — as specified in Phase 2 Role-to-Navigation mapping

**Layout:**
- Page header: "Requests" (h1, 20px/600) + breadcrumb "Requests"
- Top-right action: `[+ New Request]` button (primary, accent fill) — AL and AD only
- Status filter tabs below header: All | Draft | Submitted | Accepted | Declined
- Main content: `RequestTable` with sortable columns

**Request table columns:**

| Column | Content | Sortable | Notes |
|--------|---------|----------|-------|
| ID | `REQ-YYYY-NNNNN` format (monospace 14px/400) | Yes | — |
| Request Type | Congressional Request / Mandate / Internal Proposal (14px/400) | Yes | — |
| Topic | Truncated at 60 chars (14px/400) | Yes | Full topic in tooltip on hover |
| Requester | Name or organization (14px/400) | Yes | — |
| Due Date | MM/DD/YYYY (14px/400) | Yes | Red text if past-due AND status=draft/submitted |
| Status | `RequestStatusBadge` | Yes | Draft/Submitted/Accepted/Declined |
| Actions | Context-sensitive | No | [Edit] for Draft (AL); [Review] for Submitted (AL) |

**Table sort:** Default sort by `updated_at` descending (most recent first). Clicking column header toggles ASC/DESC.

**Status filter tabs:**
- "All" shows all requests in user's scope
- Each status tab filters to that status
- Active tab: accent underline (2px) + accent text; inactive tabs: muted text
- Tab counts shown as `(N)` badge if > 0 items

**Table loading state:** 5 `SkeletonRow` components (48px height each)

**Table row click:** Navigates to `/requests/{id}` (Request Detail)

**Table empty state (no requests yet):**
- Icon: `FileText` (lucide-react, 32px, muted)
- Heading: "No requests yet." (20px/600)
- Body: "Create the first request to begin the engagement workflow." (14px/400 muted)
- CTA: `[+ New Request]` (AL/AD only — accent fill button)

**Table empty state (filter active, no results):**
- Icon: `SearchX` (lucide-react, 32px, muted)
- Heading: "No {status} requests." (20px/600)
- Body: "Try a different status filter." (14px/400 muted)
- CTA: `[Clear filter]` (ghost button)

---

### Screen 2: New/Edit Request Form

**Source:** UX-Mockup-EMS.md — Screen-03, Flow 1A

**Routes:** `/requests/new` (create) · `/requests/{id}/edit` (edit draft only)

**Access:** AL and AD only (non-AL gets 403 ForbiddenPage from Phase 2)

**Layout:**

```
Requests > New Request                           [Save as Draft]
──────────────────────────────────────────────────────────────

── REQUEST DETAILS ────────────────────────────────────────

Request Type *
[Congressional Request                              ▾]

Requester *                   Agency / Program *
[Name or organization     ]   [Agency or program name  ]

Topic *
[Brief description of the engagement topic          ]
[                                                    ]
                                                500 / 500

Due Date *
[MM/DD/YYYY                 📅]
⚠ Due date is in the past. Permitted for retrospective mandates.

Notes
[Optional — additional context or background         ]
[                                                     ]
                                               0 / 5000

── INTAKE DOCUMENT ────────────────────────────────────────

[📄 Drag and drop intake document here               ]
[   or  [Browse files]                               ]
[   PDF, DOCX, DOC, XLSX, XLS, TXT, PNG, JPG · 25MB ]

[Save as Draft]                    [Submit Request →]
```

**Sticky footer behavior:**
- Form actions bar (`[Save as Draft]` + `[Submit Request →]`) is sticky at bottom of content area on scroll
- Height: 64px; background: white; top border 1px `hsl(214 32% 91%)`
- On short screens (content fits viewport): actions render inline at form bottom (not sticky)

**Field specifications:**

| Field | Component | Required for Draft | Required for Submit | Constraints |
|-------|-----------|-------------------|--------------------|-|
| Request Type | `Select` (dropdown) | ✓ YES | ✓ YES | 3 options only |
| Requester | `Input` (text) | No | ✓ YES | ≤255 chars |
| Agency / Program | `Input` (text) | No | ✓ YES | ≤255 chars |
| Topic | `Textarea` | No | ✓ YES | ≤500 chars; char counter |
| Due Date | `Calendar` + `Popover` | No | ✓ YES | Any date; past → warning (non-blocking) |
| Notes | `Textarea` | No | No | ≤5000 chars; char counter |
| Intake Document | `IntakeFileUpload` | No | No | 1 file; ≤25MB; allowed types: PDF/DOCX/DOC/XLSX/XLS/TXT/PNG/JPG/JPEG |

**Request Type dropdown options (exact labels):**
- Congressional Request
- Mandate
- Internal Proposal

**"Save as Draft" enable condition:** `request_type` must be selected. All other fields optional.

**"Submit Request →" enable condition:** All required-for-submit fields populated AND no validation errors. Button is enabled in real-time as fields are filled.

**Validation trigger:** Field-level validation fires on `onBlur` (not on keystroke). Full form validation fires on submit attempt.

**Required field indicator:** Red asterisk `*` after label. Helper text: "Required" (12px muted) appears on first attempted submit when field is empty.

**Topic char counter:** `{current} / 500` — right-aligned 12px/400 muted; turns red at >500.

**Notes char counter:** `{current} / 5000` — right-aligned 12px/400 muted; turns red at >5000.

**Past due date warning banner:**
- Background: `hsl(45 96% 88%)` (yellow-100)
- Text: `hsl(45 90% 28%)` (yellow-800), 14px/400
- Icon: `AlertTriangle` (lucide-react, 16px, amber)
- Copy: "Due date is in the past. Permitted for retrospective mandates."
- Appears below the Due Date field when selected date < today; non-blocking

**"Save as Draft" button:**
- Style: secondary/outline (not accent-filled) — distinguishes from primary Submit
- Loading state: spinner + "Saving..." text; disabled during request
- On success: redirect to `/requests/{id}` + toast "Draft saved [HH:MM AM/PM]."

**"Submit Request →" button:**
- Style: primary (accent-filled, white text)
- Loading state: spinner + "Submitting..." text; disabled during request
- On validation failure: button stays enabled; per-field errors appear inline
- On success: redirect to `/requests/{id}` + toast "Request submitted successfully."

**Breadcrumb:**
- New: "Requests > New Request"
- Edit: "Requests > REQ-2026-NNNNN > Edit"

**Edit mode (draft only):**
- Same form layout as New
- Fields pre-populated with existing values
- Top header badge shows "Draft" status
- "Save as Draft" replaces with "Save Changes" (same behavior)
- Submit still labeled "Submit Request →"

---

### Screen 3: File Upload Component — IntakeFileUpload

**Source:** UX-Mockup-EMS.md — Screen-03 (Intake Document section), F02.4

**Placement:** Within the request form, "INTAKE DOCUMENT" section

**Drop zone dimensions:**
- Width: 100% of form container
- Min-height: 120px
- Border: 2px dashed `hsl(214 32% 91%)` (slate-200); border-radius: 8px
- Background: white (idle) → `hsl(221 83% 93%)` blue-100 (drag-over)
- Transition: `border-color 150ms ease`, `background-color 150ms ease`

**Drop zone idle state:**
```
📄  Drag and drop intake document here
    or  [Browse files]
    PDF, DOCX, DOC, XLSX, XLS, TXT, PNG, JPG · Max 25 MB
```
- `FileText` icon (lucide-react, 32px, muted color)
- "Drag and drop intake document here": 14px/400 muted
- "[Browse files]": accent link text (inline button, no border)
- File type / size hint: 12px/400 muted

**Drop zone drag-over state:**
- Border: 2px dashed `hsl(221 83% 53%)` (accent blue-600)
- Background: `hsl(221 83% 93%)` (blue-100)
- Text: "Drop to upload" (14px/400 accent)

**Upload in progress:**
- File icon + filename (14px/400)
- `FileProgressBar`: 4px thin bar below filename; accent color fill
- "Uploading..." text (12px/400 muted)

**Upload success (file confirmed):**
```
[📄 congressional-letter-2026.pdf  ✓]  [✕]
```
- Green chip: `hsl(142 71% 88%)` background, `hsl(142 70% 28%)` text
- Filename: 14px/400
- `✓` (CheckCircle icon 14px, green)
- `✕` (X icon 14px, muted) — removes the file; resets to idle state
- Replaces drop zone content (drop zone collapses to chip row)

**Error states:**
- File type not allowed: red text below zone — "File type not permitted. Allowed: PDF, DOCX, DOC, XLSX, XLS, TXT, PNG, JPG."
- File too large: red text below zone — "File exceeds maximum size of 25 MB."
- Storage error: red text — "File could not be saved. Please try again."
- Error text: 12px/400 destructive color `hsl(0 72% 38%)`

**Re-upload behavior:** Uploading a new file replaces the previous file. Drop zone briefly shows progress, then shows new filename chip. Old file chip disappears immediately on new upload start.

**Accessibility:**
- Drop zone: `role="region"` + `aria-label="Intake document upload area"`
- `[Browse files]` button: `aria-label="Browse files to upload"`
- Progress bar: `role="progressbar"` + `aria-valuenow` + `aria-valuemin="0"` + `aria-valuemax="100"`
- File confirmed chip ✕: `aria-label="Remove {filename}"`
- Error: `role="alert"` on error text

---

### Screen 4: Request Detail Page (View Mode)

**Source:** UX-Mockup-EMS.md — Screen-03 (Request Detail), Screen-04 (Gate A1 panel)

**Routes:** `/requests/{id}` (view) — redirected here after Save as Draft, Submit, or direct link

**Access:** AL, EM (read), AD, RO

**Layout:**
```
Requests > REQ-2026-NNNNN
[Request Type badge] · [Status badge] · Submitted Jan 15, 2026 at 2:34 PM by Marcus Reid

── INTAKE DOCUMENT ────────────────────────────────────
[📄 congressional-letter-2026.pdf    [Download ↓]]
— or —
[No intake document attached.]

Request Type:    Congressional Request
Requester:       Senate Committee on Finance
Topic:           Agency Budget Review 2026
Agency/Program:  Office of Budget Management
Due Date:        April 30, 2026
Notes:           [notes text — or "No notes provided." if empty]
Submitted:       January 15, 2026 at 2:34 PM by Marcus Reid

── GATE A1 DECISION ────────────────────────────────────
[A1 decision panel — see Screen 5; or decided card if already decided]

[View Audit Trail →]
[Edit Request]  ← visible only when status=draft AND role=AL/AD
```

**Request type badge:** Small pill (12px/400, gray-100 background, gray-600 text, 4px radius)
- Congressional Request · Mandate · Internal Proposal

**Status badge:** `RequestStatusBadge` (semantic colors defined in Color section above)

**Submission timestamp:** 14px/400 muted text — "Submitted January 15, 2026 at 2:34 PM by Marcus Reid"
  - Only shown when `status ≠ draft`

**Intake document card:**
- Background: secondary `hsl(210 20% 97%)`, border 1px `hsl(214 32% 91%)`, radius 6px, padding 16px
- `FileText` icon (lucide-react, 20px, muted)
- Filename: 14px/400
- `[Download ↓]` button: secondary/outline (14px)
- When no file: "No intake document attached." (12px/400 muted)

**Field layout (read-only):**
- Label (left): 12px/400 muted, all-caps tracking-wide — "REQUEST TYPE", "REQUESTER", etc.
- Value (right or below): 14px/400 body
- Use two-column grid at desktop (≥768px); single column on mobile
- Gap between label-value pairs: 16px (`md`)

**[View Audit Trail →]:** 14px/400 accent link, right-aligned, bottom of request detail

**[Edit Request]:** secondary/outline button; visible ONLY when `status = draft` AND user role = AL or AD

---

### Screen 5: Gate A1 Decision Panel (Embedded in Request Detail)

**Source:** UX-Mockup-EMS.md — Screen-04, Flow 1B

**Visibility rule:** Rendered ONLY when `request.status = submitted` AND current user has role `AL`.
Non-AL users see the request detail without this panel.

**Panel anatomy:**
```
── GATE A1: ACCEPTANCE DECISION ────────────────────────────────

┌──────────────────────────────────────────────────────────────┐
│  ⚖  Record your acceptance decision for this request.        │
│     This decision is permanent and creates an audit event.   │
│                                                              │
│  Risk Level *                                                │
│  ○ Low    ○ Medium    ○ High                                 │
│                                                              │
│  Decision Rationale *                                        │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ e.g., Scope aligns with mandate; medium risk based on  │  │
│  │ timeline and agency response capacity.                  │  │
│  └────────────────────────────────────────────────────────┘  │
│  Minimum 10 characters [8 / 10] ← char count indicator       │
│                                                              │
│  [✗ Decline Request]              [✓ Approve Request →]      │
│  (requires rationale)             (requires risk + rationale)│
└──────────────────────────────────────────────────────────────┘
```

**Panel styling:**
- Background: white, border 1px `hsl(214 32% 91%)`, border-radius 8px, padding 24px (`lg`)
- Left border accent bar: 4px solid `hsl(221 83% 53%)` (accent blue-600)
- Section separator above panel: `Separator` component

**Risk Level radio group:**
- 3 `RadioGroup` items: Low / Medium / High
- Horizontal layout at desktop; vertical on mobile
- Each option: radio button (16px) + label (14px/400), gap 8px
- Selected state: accent-filled radio button
- Error state (approve clicked without selection): red text below group — "Risk level is required for A1 approval."

**Decision Rationale textarea:**
- Height: 100px (4 rows approx)
- Placeholder: "e.g., Scope aligns with mandate; medium risk based on timeline and agency response capacity."
- Char count indicator: `CharCountIndicator` — "X / 10" right-aligned 12px
  - Below minimum (< 10 chars): red text
  - At or above minimum (≥ 10 chars): accent dot indicator (●) + muted text
- Border: red on error (`hsl(0 72% 51%)`); accent ring on focus (2px)
- Error message: "Minimum 10 characters." (12px/400 destructive)

**Approve button:**
- Label: "✓ Approve Request →"
- Style: primary (accent-filled, white text), 40px height, min-width 180px
- Enabled when: risk_level selected AND rationale ≥ 10 chars
- Loading state: spinner + "Processing..." disabled
- On click (valid): opens `ApproveConfirmDialog`

**Decline button:**
- Label: "✗ Decline Request"
- Style: outline with destructive border/text (`hsl(0 72% 51%)`)
- Enabled when: rationale ≥ 10 chars (risk_level NOT required for decline)
- Loading state: spinner + "Processing..." disabled
- On click (valid): opens `DeclineConfirmDialog`

**Button layout:** Decline left, Approve right. Gap: 16px (`md`). Both buttons full-height 40px.

---

### Screen 6: Approve Confirmation Dialog

**Source:** UX-Mockup-EMS.md — Screen-04 Confirmation Dialog — Approve

**Component:** shadcn `AlertDialog` (cannot dismiss by clicking outside — irreversible action)

```
Approve Request

You are about to approve this request:
"{topic truncated at 80 chars}"

Risk Level: [Medium badge]

This will:
• Create a new Engagement Shell (ENG-{YYYY}-NNNNN)
• Record your decision with a timestamp
• This action cannot be undone.

          [Keep Request Pending]    [Confirm Approve ✓]
```

**Dimensions:** max-width 520px

**Content:**
- Title: "Approve Request" (20px/600)
- Body: request topic name in quotes + risk level badge + bullet list (14px/400)
- Risk level badge: shown inline with same colors as `RequestStatusBadge` risk variants
- "[Keep Request Pending]": ghost button
- "[Confirm Approve ✓]": primary accent-filled button
- Loading: "Confirm Approve ✓" → spinner + "Processing..." disabled

**Post-approval (after dialog closes):**
- User redirected to new Engagement Shell `/engagements/{id}`
- Green success banner on Engagement Shell: "✅ Engagement ENG-YYYY-NNNNN created. [View Engagement Shell →]"
  - Background: `hsl(142 71% 88%)` (green-100), text `hsl(142 70% 28%)` (green-800)
  - Dismissible: `✕` close button (top-right of banner)

---

### Screen 7: Decline Confirmation Dialog

**Source:** UX-Mockup-EMS.md — Screen-04 Confirmation Dialog — Decline

**Component:** shadcn `AlertDialog`

```
Decline Request

You are about to decline this request:
"{topic truncated at 80 chars}"

No engagement will be created.
This decision is permanent.

          [Keep Request Pending]    [Confirm Decline ✗]
```

**Dimensions:** max-width 480px

**Content:**
- Title: "Decline Request" (20px/600)
- Body: topic name in quotes + plain warning sentences (14px/400)
- "[Keep Request Pending]": ghost button
- "[Confirm Decline ✗]": destructive button (red fill `hsl(0 72% 51%)`, white text)
- Loading: spinner + "Processing..." disabled

**Post-decline:**
- User redirected to `/requests` (Requests list)
- Toast: "Request declined and recorded." (green, 4s auto-dismiss)

---

### Screen 8: Post-Decision Read-Only Card (A1 Decided State)

**Source:** UX-Mockup-EMS.md — Screen-04 Post-Decision States

**Replaces:** The `GateA1Panel` once a decision has been made (for any viewer).

**Approved state:**
```
── GATE A1 DECISION (Read-Only) ──────────────────────────────
[✓ Approved]  Risk Level: [Medium]
Approver:  Marcus Reid
Date:      January 15, 2026 at 2:41 PM
Rationale: "Scope aligns with mandate; medium risk based on timeline
            and agency response capacity."
[View Gate History →]
```

**Declined state:**
```
── GATE A1 DECISION (Read-Only) ──────────────────────────────
[✗ Declined]
Approver:  Marcus Reid
Date:      January 15, 2026 at 2:41 PM
Rationale: "Scope is outside current mandate boundaries per legal
            review. Declined pending clarification."
[View Gate History →]
```

**Card styling:**
- Background: `hsl(210 20% 97%)` (secondary — read-only surface)
- Border: 1px `hsl(214 32% 91%)`, radius 6px, padding 16px (`md`)
- Status chip: `RequestStatusBadge` (accepted = green-100; declined = red-100)
- All text: 14px/400; labels 12px/400 muted uppercase tracking-wide
- "[View Gate History →]": 12px/400 accent link

---

### Screen 9: Request Submitted Confirmation State

**Source:** UX-Mockup-EMS.md — Flow 1A exit state

Rendered on the **Request Detail page** immediately after successful submission (redirect from form).
The page header shows:
- Status badge switches from "Draft" → "Submitted"
- Toast (top-right, green, 4s): "Request submitted successfully."
- [Edit Request] button is no longer visible (request is locked)
- A1 Decision Panel placeholder: "Awaiting Gate A1 decision." (14px/400 muted) if no decision yet

No separate confirmation "screen" — it is the standard Request Detail page in Submitted state.

---

### Screen 10: Empty States

| Context | Icon | Heading | Body | CTA |
|---------|------|---------|------|-----|
| Request list (no requests) | `FileText` 32px muted | "No requests yet." | "Create the first request to begin the engagement workflow." | "+ New Request" (AL/AD only) |
| Request list (status filter, no results) | `SearchX` 32px muted | "No {status} requests." | "Try a different status filter." | "Clear filter" (ghost) |
| A1 Review Queue (empty) | `ClipboardCheck` 32px muted | "No items awaiting your review." | "Submitted requests will appear here for Gate A1 decision." | none |

---

## Copywriting Contract

### Primary CTAs

| Element | Copy | Context |
|---------|------|---------|
| New request CTA | "+ New Request" | Request list top-right; empty state button |
| Save draft CTA | "Save as Draft" | New request form (draft save) |
| Save draft (edit mode) | "Save Changes" | Edit draft form |
| Submit request CTA | "Submit Request →" | New/edit form primary action |
| Approve CTA | "✓ Approve Request →" | Gate A1 panel approve button |
| Decline CTA | "✗ Decline Request" | Gate A1 panel decline button |
| Confirm approve | "Confirm Approve ✓" | Confirmation dialog CTA |
| Confirm decline | "Confirm Decline ✗" | Confirmation dialog CTA (destructive) |
| Approve dialog cancel | "Keep Request Pending" | Ghost button — dismisses dialog without taking gate action |
| Decline dialog cancel | "Keep Request Pending" | Ghost button — dismisses dialog without taking gate action |
| Download intake doc | "Download ↓" | Request detail intake document card |
| View audit trail | "View Audit Trail →" | Request detail bottom link |
| View engagement | "View Engagement Shell →" | Post-approval success banner |
| View gate history | "View Gate History →" | A1 decided card link |

### Loading States

| Element | Loading Copy |
|---------|-------------|
| Save as Draft button | "Saving..." (+ spinner) |
| Submit Request button | "Submitting..." (+ spinner) |
| Confirm Approve button | "Processing..." (+ spinner) |
| Confirm Decline button | "Processing..." (+ spinner) |
| Request table | 5 skeleton rows (no copy) |

### Toasts

| Event | Toast Copy | Color | Duration |
|-------|-----------|-------|----------|
| Draft saved | "Draft saved [HH:MM AM/PM]." | green | 4s |
| Request submitted | "Request submitted successfully." | green | 4s |
| Request declined | "Request declined and recorded." | green | 4s |

### Banners

| Event | Banner Copy | Color |
|-------|------------|-------|
| Approval success | "✅ Engagement ENG-YYYY-NNNNN created. [View Engagement Shell →]" | green-100 bg |
| Past due date | "Due date is in the past. Permitted for retrospective mandates." | yellow-100 bg |
| File type error | "File type not permitted. Allowed: PDF, DOCX, DOC, XLSX, XLS, TXT, PNG, JPG." | red text |
| File size error | "File exceeds maximum size of 25 MB." | red text |
| Storage error | "File could not be saved. Please try again." | red text |

### Validation Error Messages (per field)

| Field | Error | Source |
|-------|-------|--------|
| Request Type (submit) | "Request type is required." | FRD F02.3 |
| Requester (submit) | "Requester is required." | FRD F02.3 |
| Topic (submit) | "Topic is required." | FRD F02.3 |
| Topic (length) | "Topic must be 500 characters or fewer." | FRD F02.1 |
| Agency/Program (submit) | "Agency / Program is required." | FRD F02.3 |
| Due Date (submit) | "Due date is required." | FRD F02.3 |
| Risk Level (approve) | "Risk level is required for A1 approval." | FRD F03.2 / UX Mockup |
| Rationale (approve/decline, too short) | "Minimum 10 characters." | FRD F03.2, F03.3 |
| Rationale (decline, missing) | "Rationale is required to decline." | FRD F03.3 |

### Empty State Copy

| Screen | Heading | Body | CTA |
|--------|---------|------|-----|
| Request list (no requests) | "No requests yet." | "Create the first request to begin the engagement workflow." | "+ New Request" (AL/AD) |
| Request list (status filtered, no results) | "No {status} requests." | "Try a different status filter." | "Clear filter" |
| Review Queue (no A1 items) | "No items awaiting your review." | "Submitted requests will appear here for Gate A1 decision." | none |

### Confirmation Dialogs (Destructive Actions)

| Action | Title | Body | Confirm Button |
|--------|-------|------|----------------|
| Approve request | "Approve Request" | "You are about to approve '{topic}'. Risk Level: {level}. This will: Create a new Engagement Shell (ENG-{YYYY}-NNNNN) · Record your decision with a timestamp · This action cannot be undone." | "Confirm Approve ✓" (primary accent) |
| Decline request | "Decline Request" | "You are about to decline '{topic}'. No engagement will be created. This decision is permanent." | "Confirm Decline ✗" (destructive red) |

> Both confirmation dialogs use shadcn `AlertDialog` which blocks outside-click dismissal.
> "Keep Request Pending" is the ghost button in both dialogs — preserves the request in its current pending/submitted state.

---

## Interaction States Summary

### Request Form States

| State | Visual Change | Copy |
|-------|--------------|------|
| New (empty) | All fields blank; "Save as Draft" disabled until request_type selected | — |
| Request type selected | "Save as Draft" enables | — |
| All required fields filled | "Submit Request →" enables | — |
| Saving (draft) | Save button spinner | "Saving..." |
| Submitting | Submit button spinner | "Submitting..." |
| Validation failure | Red border + error text per field below input | Per-field error messages |
| Draft saved | Redirect to Request Detail + toast | "Draft saved {time}." |
| Submitted | Redirect to Request Detail (submitted state) + toast | "Request submitted successfully." |
| Past due date | Yellow warning banner below Due Date field | "Due date is in the past..." |

### File Upload States

| State | Visual Change | Copy |
|-------|--------------|------|
| Idle | Dashed border, file icon, instructions | "Drag and drop intake document here" |
| Drag over | Accent border, blue-100 background | "Drop to upload" |
| Uploading | Progress bar + filename | "Uploading..." |
| Success | Green filename chip + ✓ + ✕ | filename.ext |
| Type error | Red error text below zone | "File type not permitted..." |
| Size error | Red error text below zone | "File exceeds maximum size of 25 MB." |
| Storage error | Red error text below zone | "File could not be saved. Please try again." |

### Gate A1 Panel States

| State | Visual Change | Copy |
|-------|--------------|------|
| Undecided (AL viewing submitted) | Full panel visible; both buttons present | — |
| No risk level (Approve clicked) | Radio group error state | "Risk level is required for A1 approval." |
| Rationale too short | Red char counter, error message below textarea | "Minimum 10 characters." |
| Both inputs valid | Both buttons fully active | — |
| Confirming approve | `ApproveConfirmDialog` open | — |
| Confirming decline | `DeclineConfirmDialog` open | — |
| Processing approval | Confirm button spinner | "Processing..." |
| Processing decline | Confirm button spinner | "Processing..." |
| Approved (any viewer) | Panel replaced by `GateA1DecidedCard` (green border accent) | Read-only decision card |
| Declined (any viewer) | Panel replaced by `GateA1DecidedCard` (red border variant) | Read-only decision card |
| Non-AL viewing submitted | A1 panel NOT rendered (absent from DOM) | Placeholder: "Awaiting Gate A1 decision." |

### Request Status Tab Filter States

| State | Visual Change |
|-------|--------------|
| All tab active | Accent underline on "All"; all requests shown |
| Status tab active | Accent underline on active tab; table filtered |
| Tab with zero items | Tab still shown; table shows empty-state |
| Loading | Skeleton rows during filter change |

---

## Layout Constants (Phase 3 — extends Phase 1 and 2)

All Phase 1 and Phase 2 layout constants are inherited unchanged. Phase 3 additions:

| Constant | Value | Usage |
|----------|-------|-------|
| File upload drop zone min-height | 120px | Drop zone minimum target area |
| File upload progress bar height | 4px | Thin progress indicator (not shadcn default height) |
| A1 panel left accent bar width | 4px | Accent border-left on Gate A1 decision panel |
| Form max-width | 720px | New/Edit Request form content max-width (centered in content area) |
| Form section gap | 24px (`lg`) | Gap between "REQUEST DETAILS" and "INTAKE DOCUMENT" sections |
| Sticky form actions bar height | 64px | Same as top bar (Phase 1 constant) |
| Confirmation dialog max-width (approve) | 520px | Approve request AlertDialog |
| Confirmation dialog max-width (decline) | 480px | Decline request AlertDialog |
| Rationale textarea min-height | 100px | ≈4 rows at 14px line-height 1.5 |
| Request table row height | 48px | Touch-target compliant row height |
| Request detail field grid | 2-column (label + value) at ≥768px | Read-only field layout |
| Status filter tab bar height | 40px | Tabs component height |
| Risk level radio group height | 40px | Radio group touch target |

---

## Responsive Behavior (Phase 3)

| Breakpoint | Request Form | Request List | Request Detail | A1 Panel |
|------------|-------------|-------------|----------------|----------|
| Desktop (>1024px) | 720px max-width centered; two-column field row (Requester + Agency) | Full 8-column table | Two-column field grid | Full panel layout |
| Tablet (768–1024px) | Full-width; two-column field row retained | Priority columns + scroll | Two-column field grid | Full panel layout |
| Mobile (<768px) | Full-width; stacked single column (Requester above Agency) | Stacked card per request | Single-column field list | Stacked: radio group → textarea → buttons |

**Mobile form actions:** Sticky footer bar — both buttons stack vertically (Submit on top, Save as Draft below) at <480px.

**Mobile request table:** Each row collapses to a card showing: ID (monospace 12px) + Topic (14px/600) + Status badge + Due date (12px muted). Tap card → Request Detail.

---

## Visuals

### Visual Hierarchy

Each major surface has one primary visual anchor — the single element that draws the user's eye first and signals the available action.

- **Primary visual anchor (Request List):** the `[+ New Request]` accent-filled button in the top-right action area.
- **Primary visual anchor (New/Edit Request Form):** the `[Submit Request →]` accent-filled sticky footer CTA.
- **Primary visual anchor (Gate A1 Panel):** the 4px accent-colored left border bar and the `[✓ Approve Request →]` accent-filled primary button.

All other interactive elements on these surfaces use secondary (outline) or ghost button styles so the primary anchor reads as the clear call-to-action.

---

## Accessibility Contract (Phase 3 — extends Phase 1 and 2)

Phase 1 and 2 accessibility contracts are fully inherited. Phase 3 additions:

| Requirement | Specification | Source |
|-------------|---------------|--------|
| File upload drag zone | `role="region"` + `aria-label="Intake document upload area"` | WCAG 2.1 — region landmark |
| File upload keyboard access | `[Browse files]` button focusable; Enter/Space triggers file picker | WCAG 2.1 — keyboard nav |
| File upload progress | `role="progressbar"` + `aria-valuenow` + `aria-valuemin="0"` + `aria-valuemax="100"` | WCAG 2.1 |
| File upload success/error | `role="status"` on success; `role="alert"` on error (live region) | WCAG 2.1 |
| Form required fields | `aria-required="true"` on all required inputs; error state `aria-invalid="true"` | WCAG 2.1 |
| Character count | `aria-live="polite"` on char counter; not announced on every keystroke (debounced 1s) | WCAG 2.1 |
| Rationale textarea | `aria-describedby` linking textarea to char count indicator | WCAG 2.1 |
| Risk level radio group | `<fieldset>` + `<legend>Risk Level</legend>`; error message `aria-describedby` | WCAG 2.1 |
| Approve/Decline buttons | `aria-disabled="true"` (not `disabled` attribute) when inputs incomplete — preserves focus | WCAG 2.1 / 2.5 |
| AlertDialog focus | Focus trapped inside dialog; Escape closes (cancel behavior); returns to trigger button | shadcn AlertDialog default |
| Past due date warning | `role="alert"` on warning banner (announced when date changes to past) | WCAG 2.1 |
| Confirmation dialog title | `aria-labelledby` pointing to dialog title h2 | shadcn AlertDialog default |
| Status badge | `aria-label="Status: {status}"` on badge element for screen readers | WCAG 2.1 |
| Download link | `aria-label="Download {filename}"` on download button | WCAG 2.1 |
| Request table | `<table>` with proper `<thead>`, `<th scope="col">`, sortable columns include `aria-sort` | WCAG 2.1 |
| Sort controls | `aria-sort="ascending"` / `"descending"` / `"none"` on column headers | WCAG 2.1 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `form`, `select`, `textarea`, `calendar`, `popover`, `progress`, `tabs`, `table`, `alert`, `separator`, `badge`, `radio-group` | not required — official registry; no third-party code |
| Third-party | none | not applicable |

> All components are from the official shadcn registry at `https://ui.shadcn.com`.
> No third-party registries are used or planned in Phase 3.
> Registry safety gate: **view passed — no flags — 2026-06-05** (official registry; no audit required per policy)
>
> Phase 2 components already installed that Phase 3 reuses:
> `input`, `button`, `dialog`, `alert-dialog`, `toast`, `skeleton`, `breadcrumb`, `command`
> — these do not need re-installation.

---

## Pre-Population Sources

| Decision | Value | Source |
|----------|-------|--------|
| Request fields (required for submit vs. draft) | request_type only required for draft; all 5 fields for submit | FRD F02.1, F02.3 |
| File type allowlist (intake document) | PDF, DOCX, DOC, XLSX, XLS, TXT, PNG, JPG, JPEG | FRD F02.4 / US-2.3 |
| Max file size | 25 MB | FRD F02.4 / US-2.3 |
| One file per request (replace behavior) | Single file; new upload replaces old | FRD F02.4 / US-2.3 |
| A1 role restriction | Only AL role sees/uses A1 decision controls | FRD F03.1, F03.2 |
| Risk level options | Low / Medium / High | FRD F03.2 / US-3.2 |
| Rationale minimum characters | 10 characters | FRD F03.2, F03.3 / US-3.2, US-3.3 |
| Approve: confirmation dialog required | Both approve and decline require AlertDialog | UX-Mockup-EMS.md Screen-04 |
| Post-approve redirect | To new Engagement Shell `/engagements/{id}` | FRD F03.2 / US-3.2 |
| Post-decline redirect | To Requests list | UX-Mockup-EMS.md Flow 1B |
| Job code pattern | `ENG-{YYYY}-{5-digit-seq}` | FRD F03.4 / US-3.2 |
| Engagement created with phase=planning | Auto-created shell on approval | FRD F03.4 |
| Request list sort order | Updated_at descending (most recent first) | researcher default (governance tools) |
| Status filter tabs | All / Draft / Submitted / Accepted / Declined | PRD F2, FRD F02 status enum |
| Request type display labels | Congressional Request / Mandate / Internal Proposal | FRD F02.1 enum values (humanized) |
| Due date past-date warning | Non-blocking yellow warning | FRD F02.3 / US-2.2 |
| Topic max chars | 500 | FRD F02.1 |
| Notes max chars | 5000 | FRD F02.1 |
| A1 panel absent from DOM for non-AL | Not disabled — entirely absent | UX-Mockup-EMS.md Screen-04 Key UX Rules |
| Confirmation dialog: blocks outside-click | shadcn AlertDialog behavior | UX-Mockup-EMS.md Pattern: Confirmation Dialogs |
| Status badge colors (request) | Draft=gray, Submitted=yellow, Accepted=green, Declined=red | researcher derived from gate color system |
| Risk level badge colors | Low=green, Medium=yellow, High=red | researcher default (traffic-light convention) |
| Spacing scale | Inherited 8-point scale | Phase 1 UI-SPEC |
| Typography | Inherited 4 sizes, 2 weights | Phase 1 UI-SPEC |
| Color tokens | Inherited (accent blue-600, secondary slate-50, dominant white) | Phase 1 UI-SPEC |
| shadcn component tokens | Inherited Phase 2 CSS variable set | Phase 2 UI-SPEC |
| Layout constants (sidebar, topbar, content) | Inherited | Phase 1 UI-SPEC |
| Toast behavior | Top-right, 4s auto-dismiss | Phase 2 UI-SPEC |
| AlertDialog pattern | Cannot close by backdrop click | Phase 2 UI-SPEC |
| WCAG 2.1 AA target | All interactions meet AA | Phase 2 UI-SPEC |
| Form validation trigger | onBlur per field; full validation on submit | UX-Mockup-EMS.md Screen-03 |
| Sticky form actions bar | 64px height, bottom of content area on scroll | UX-Mockup-EMS.md Screen-03 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
