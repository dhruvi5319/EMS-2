---
phase: 02-application-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components.json
  - frontend/tailwind.config.ts
  - frontend/src/index.css
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/toast.tsx
  - frontend/src/components/ui/toaster.tsx
  - frontend/src/components/ui/use-toast.ts
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/alert-dialog.tsx
  - frontend/src/components/ui/table.tsx
  - frontend/src/components/ui/checkbox.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/command.tsx
  - frontend/src/components/ui/breadcrumb.tsx
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/TopBar.tsx
  - frontend/src/main.tsx
autonomous: true

features:
  implements: ["F0"]
  depends_on: []
  enables: ["F0"]

must_haves:
  truths:
    - "shadcn/ui is initialized: components.json exists, src/components/ui/ has all 12 required component files"
    - "tailwind.config.ts exports CSS variable mappings aligned exactly to Phase 1 color tokens"
    - "Phase 1 plain-Tailwind components (LoginCard, AppShell, Sidebar, TopBar) are migrated to use shadcn tokens without visual regression"
    - "The Toaster component is mounted in main.tsx so toast() calls work globally"
    - "All shadcn components import from @/components/ui/* path alias resolving to frontend/src/components/ui/"
  artifacts:
    - path: "frontend/components.json"
      provides: "shadcn config — New York style, CSS variables, blue-600 accent"
      contains: "new-york"
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind config with CSS variable color mappings"
      contains: "hsl(var(--primary))"
    - path: "frontend/src/index.css"
      provides: "CSS variable definitions for all shadcn tokens"
      contains: "--primary: 221 83% 53%"
    - path: "frontend/src/components/ui/command.tsx"
      provides: "shadcn Command component for global search overlay"
      exports: ["Command", "CommandInput", "CommandList", "CommandItem", "CommandEmpty", "CommandGroup"]
    - path: "frontend/src/components/ui/dialog.tsx"
      provides: "shadcn Dialog for Create User / Edit Roles modals"
      exports: ["Dialog", "DialogContent", "DialogHeader", "DialogTitle", "DialogFooter"]
    - path: "frontend/src/components/ui/alert-dialog.tsx"
      provides: "shadcn AlertDialog for Deactivate User confirmation"
      exports: ["AlertDialog", "AlertDialogContent", "AlertDialogTitle", "AlertDialogDescription", "AlertDialogFooter", "AlertDialogCancel", "AlertDialogAction"]
    - path: "frontend/src/components/ui/toast.tsx"
      provides: "shadcn Toast for success/error notifications"
      exports: ["Toast", "ToastProvider", "ToastViewport"]
    - path: "frontend/src/components/ui/table.tsx"
      provides: "shadcn Table for user list + audit trail"
      exports: ["Table", "TableHeader", "TableBody", "TableRow", "TableHead", "TableCell"]
    - path: "frontend/src/components/ui/checkbox.tsx"
      provides: "shadcn Checkbox for role assignment"
      exports: ["Checkbox"]
    - path: "frontend/src/components/ui/select.tsx"
      provides: "shadcn Select for audit trail filters"
      exports: ["Select", "SelectTrigger", "SelectContent", "SelectItem", "SelectValue"]
  key_links:
    - from: "frontend/src/index.css"
      to: "frontend/tailwind.config.ts"
      via: "CSS variables consumed by Tailwind theme extension"
      pattern: "--primary.*221.*83"
    - from: "frontend/src/main.tsx"
      to: "frontend/src/components/ui/toaster.tsx"
      via: "Toaster mounted inside BrowserRouter; toast() calls work from anywhere"
      pattern: "Toaster"
    - from: "frontend/src/components/layout/AppShell.tsx"
      to: "shadcn token classes"
      via: "bg-background, text-foreground, border-border — Tailwind CSS variable classes"
      pattern: "bg-background|text-foreground|border-border"

integration_contracts:
  requires: []
  provides:
    - artifact: "frontend/components.json"
      exports: ["shadcn config — New York, CSS variables, blue-600 accent, path alias @/components/ui"]
      shape: |
        { "style": "new-york", "rsc": false, "tsx": true, "tailwind": { "cssVariables": true },
          "aliases": { "components": "@/components", "utils": "@/lib/utils" } }
      verify: "grep -n 'new-york' frontend/components.json && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/command.tsx"
      exports: ["Command", "CommandInput", "CommandList", "CommandItem", "CommandEmpty", "CommandGroup"]
      shape: "shadcn Command — keyboard nav (arrow keys, Enter, Escape), cmdk-based"
      verify: "grep -n 'CommandInput\|CommandList\|CommandItem' frontend/src/components/ui/command.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/dialog.tsx"
      exports: ["Dialog", "DialogContent", "DialogHeader", "DialogTitle", "DialogFooter", "DialogTrigger", "DialogClose"]
      shape: "shadcn Dialog — focus-trapped modal, Radix Dialog primitive"
      verify: "grep -n 'DialogContent\|DialogHeader\|DialogTitle' frontend/src/components/ui/dialog.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/alert-dialog.tsx"
      exports: ["AlertDialog", "AlertDialogContent", "AlertDialogTitle", "AlertDialogDescription", "AlertDialogFooter", "AlertDialogCancel", "AlertDialogAction", "AlertDialogTrigger"]
      shape: "shadcn AlertDialog — cannot dismiss by backdrop click, focus-trapped"
      verify: "grep -n 'AlertDialogContent\|AlertDialogAction\|AlertDialogCancel' frontend/src/components/ui/alert-dialog.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/table.tsx"
      exports: ["Table", "TableHeader", "TableBody", "TableRow", "TableHead", "TableCell", "TableCaption"]
      shape: "shadcn Table — semantic HTML table primitives"
      verify: "grep -n 'TableHeader\|TableBody\|TableRow\|TableCell' frontend/src/components/ui/table.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/toast.tsx"
      exports: ["Toast", "ToastProvider", "ToastViewport", "ToastTitle", "ToastDescription", "ToastClose", "ToastAction"]
      shape: "shadcn Toast — Radix Toast primitive, 4s auto-dismiss"
      verify: "grep -n 'ToastProvider\|ToastViewport' frontend/src/components/ui/toast.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/index.css"
      exports: ["CSS variables: --primary, --secondary, --background, --foreground, --border, --muted, --muted-foreground, --destructive, --ring"]
      shape: |
        :root { --primary: 221 83% 53%; --secondary: 210 20% 97%; --background: 0 0% 100%;
          --foreground: 222 47% 11%; --border: 214 32% 91%; --muted: 210 40% 96.1%;
          --muted-foreground: 215 16% 47%; --destructive: 0 72% 51%; --ring: 221 83% 53%; }
      verify: "grep -n 'primary: 221 83' frontend/src/index.css && echo CONTRACT_OK"
---

<objective>
Initialize shadcn/ui with New York style and CSS variables, install all 12 required shadcn components, configure Tailwind CSS variable mappings aligned to Phase 1 color tokens, and migrate Phase 1 plain-Tailwind components to shadcn token classes.

Purpose: shadcn must be initialized before any Phase 2 component work. This is the foundation plan that all other Phase 2 plans depend on for UI primitives (Command, Dialog, AlertDialog, Table, Toast, etc.).
Output: components.json, src/components/ui/* (12 components), updated tailwind.config.ts with CSS variable mappings, updated index.css with HSL token definitions, migrated AppShell/Sidebar/TopBar.
</objective>

<feature_dependencies>
Implements: F0: Application shell foundation — shadcn initialization enabling all Phase 2 UI components
Depends on: None (wave 1 — no prior phase 2 plans required; Phase 1 scaffold must exist)
Enables: F0 full navigation (02-02), F0 global search (02-04), F0 user management (02-05), F0 audit trail (02-06)
</feature_dependencies>

<execution_context>
@/root/.config/opencode/pivota_spec-framework/workflows/execute-plan.md
@/root/.config/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-application-shell/02-UI-SPEC.md
@.planning/phases/01-foundation/01-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn, configure CSS variables, install all 12 required components</name>
  <files>
    frontend/components.json
    frontend/tailwind.config.ts
    frontend/src/index.css
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/components/ui/toast.tsx
    frontend/src/components/ui/toaster.tsx
    frontend/src/components/ui/use-toast.ts
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/alert-dialog.tsx
    frontend/src/components/ui/table.tsx
    frontend/src/components/ui/checkbox.tsx
    frontend/src/components/ui/select.tsx
    frontend/src/components/ui/command.tsx
    frontend/src/components/ui/breadcrumb.tsx
    frontend/src/lib/utils.ts
  </files>
  <action>
Run shadcn init and add all required components. Then configure CSS variables to match Phase 1 token contract exactly.

**Step 1: Ensure dependencies are installed**
```bash
cd frontend
# Install cmdk (shadcn Command dependency) and class-variance-authority if not present
npm install cmdk class-variance-authority clsx tailwind-merge @radix-ui/react-dialog @radix-ui/react-alert-dialog @radix-ui/react-checkbox @radix-ui/react-select @radix-ui/react-toast @radix-ui/react-slot tailwindcss-animate
```

**Step 2: Initialize shadcn with exact settings from 02-UI-SPEC.md**

Create `frontend/components.json` (shadcn config — do NOT run interactive init which may fail in CI; write config directly):
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/index.css",
    "baseColor": "slate",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

**Step 3: Update `frontend/tailwind.config.ts`** — extend with CSS variable theme mapping.

Replace or update the tailwind config to add `extend.colors` using `hsl(var(...))` pattern, and add `tailwindcss-animate` plugin. The config MUST also preserve the existing content array for Phase 1 components.

```typescript
import type { Config } from 'tailwindcss';
import animate from 'tailwindcss-animate';

const config: Config = {
  darkMode: ['class'],
  content: [
    './index.html',
    './src/**/*.{ts,tsx}',
  ],
  theme: {
    container: {
      center: true,
      padding: '2rem',
      screens: {
        '2xl': '1400px',
      },
    },
    extend: {
      colors: {
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))',
        },
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
      keyframes: {
        'accordion-down': {
          from: { height: '0' },
          to: { height: 'var(--radix-accordion-content-height)' },
        },
        'accordion-up': {
          from: { height: 'var(--radix-accordion-content-height)' },
          to: { height: '0' },
        },
      },
      animation: {
        'accordion-down': 'accordion-down 0.2s ease-out',
        'accordion-up': 'accordion-up 0.2s ease-out',
      },
    },
  },
  plugins: [animate],
};

export default config;
```

**Step 4: Update `frontend/src/index.css`** — define CSS variables aligned EXACTLY to Phase 1 color contract from 02-UI-SPEC.md.

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* Dominant (white — 60%) */
    --background: 0 0% 100%;
    --foreground: 222 47% 11%;

    /* Card (white) */
    --card: 0 0% 100%;
    --card-foreground: 222 47% 11%;

    /* Popover (white) */
    --popover: 0 0% 100%;
    --popover-foreground: 222 47% 11%;

    /* Primary (blue-600 accent — 10%) */
    --primary: 221 83% 53%;
    --primary-foreground: 0 0% 100%;

    /* Secondary (slate-50 — 30%) */
    --secondary: 210 20% 97%;
    --secondary-foreground: 222 47% 11%;

    /* Muted (slate-100 — surface elevated) */
    --muted: 210 40% 96.1%;
    --muted-foreground: 215 16% 47%;

    /* Accent (slate-100 — hover surfaces) */
    --accent: 210 40% 96.1%;
    --accent-foreground: 222 47% 11%;

    /* Destructive (red-600) */
    --destructive: 0 72% 51%;
    --destructive-foreground: 0 0% 100%;

    /* Border (slate-200) */
    --border: 214 32% 91%;
    --input: 214 32% 91%;

    /* Focus ring (blue-600) */
    --ring: 221 83% 53%;

    /* Border radius base */
    --radius: 0.5rem;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
    font-family: Inter, ui-sans-serif, system-ui, -apple-system, sans-serif;
    font-feature-settings: "rlig" 1, "calt" 1;
  }
}
```

**Step 5: Create `frontend/src/lib/utils.ts`** (shadcn utility — required by all components):
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**Step 6: Add shadcn components via CLI**

Run the following to add all required components. shadcn CLI reads `components.json` and writes to `src/components/ui/`:
```bash
cd frontend
npx shadcn add button input badge skeleton toast dialog alert-dialog table checkbox select command breadcrumb --yes --overwrite
```

If the CLI fails (network issues, interactive prompts), manually create each component file by fetching from the shadcn registry:
```bash
# Fallback: fetch component source directly
npx shadcn@latest add button --yes --overwrite 2>/dev/null || \
  curl -s https://ui.shadcn.com/r/button.json | node -e "const d=require('fs');const j=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));j.files.forEach(f=>d.writeFileSync('src/'+f.path,f.content));"
```

After the CLI runs, verify `src/components/ui/` contains at minimum: `button.tsx`, `input.tsx`, `badge.tsx`, `skeleton.tsx`, `toast.tsx`, `toaster.tsx`, `use-toast.ts`, `dialog.tsx`, `alert-dialog.tsx`, `table.tsx`, `checkbox.tsx`, `select.tsx`, `command.tsx`, `breadcrumb.tsx`.

**Step 7: Ensure path alias `@/` resolves to `src/`**

Update `frontend/tsconfig.json` to add path alias (if not present from Phase 1):
```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Update `frontend/vite.config.ts` to add resolve alias (if not present):
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    proxy: {
      '/api': 'http://localhost:3001',
    },
  },
});
```
  </action>
  <verify>
grep -n 'new-york' frontend/components.json && echo CONTRACT_OK
grep -n 'primary: 221 83' frontend/src/index.css && echo CONTRACT_OK
grep -n 'hsl(var(--primary))' frontend/tailwind.config.ts && echo CONTRACT_OK
ls frontend/src/components/ui/command.tsx frontend/src/components/ui/dialog.tsx frontend/src/components/ui/alert-dialog.tsx frontend/src/components/ui/table.tsx frontend/src/components/ui/toast.tsx frontend/src/components/ui/checkbox.tsx frontend/src/components/ui/select.tsx && echo "ALL_COMPONENTS_PRESENT"
grep -n 'CommandInput\|CommandList\|CommandItem' frontend/src/components/ui/command.tsx && echo CONTRACT_OK
grep -n 'DialogContent\|DialogHeader\|DialogTitle' frontend/src/components/ui/dialog.tsx && echo CONTRACT_OK
grep -n 'AlertDialogContent\|AlertDialogAction\|AlertDialogCancel' frontend/src/components/ui/alert-dialog.tsx && echo CONTRACT_OK
  </verify>
  <done>
`frontend/components.json` exists with `"style": "new-york"` and `"cssVariables": true`. `frontend/src/index.css` defines all CSS variables with `--primary: 221 83% 53%` (blue-600) matching Phase 1 token contract. `frontend/tailwind.config.ts` extends theme with `hsl(var(--primary))` color mappings. `frontend/src/lib/utils.ts` exports `cn()`. All 12 shadcn components exist in `frontend/src/components/ui/`: button, input, badge, skeleton, toast, toaster, use-toast, dialog, alert-dialog, table, checkbox, select, command, breadcrumb. `@/` path alias resolves to `src/` in both tsconfig.json and vite.config.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Phase 1 components to shadcn tokens + mount Toaster</name>
  <files>
    frontend/src/components/layout/AppShell.tsx
    frontend/src/components/layout/Sidebar.tsx
    frontend/src/components/layout/TopBar.tsx
    frontend/src/main.tsx
  </files>
  <action>
Migrate the Phase 1 plain-Tailwind components to use shadcn CSS variable token classes. Per UI-SPEC: "The plain-Tailwind Phase 1 components (LoginCard, NavItem, etc.) migrate to use shadcn tokens but keep their existing class structure — only token references change." Mount Toaster in main.tsx.

**Token migration mapping (Phase 1 class → shadcn token class):**
- `bg-white` → `bg-background` (same value, now via CSS variable)
- `bg-slate-50` → `bg-secondary` (same value)
- `bg-slate-100` → `bg-muted` (same value)
- `border-slate-200` → `border-border` (same value)
- `text-slate-500` → `text-muted-foreground` (same value)
- `text-blue-600` → `text-primary` (same value)
- `bg-blue-600` → `bg-primary` (same value)
- `hover:bg-slate-100` → `hover:bg-muted` (same value)
- `ring-blue-600` → `ring-ring` (same value)

**`frontend/src/components/layout/AppShell.tsx`** — update bg and structural classes:
```typescript
import { Outlet } from 'react-router-dom';
import { Sidebar } from './Sidebar';
import { TopBar } from './TopBar';

export function AppShell() {
  return (
    <div className="min-h-screen bg-background">
      <TopBar />
      <Sidebar />
      <main
        className="p-8"
        style={{ marginLeft: 220, paddingTop: 64 + 32 }}
      >
        <div className="max-w-[1280px] mx-auto">
          <Outlet />
        </div>
      </main>
    </div>
  );
}
```

**`frontend/src/components/layout/Sidebar.tsx`** — update background + border + nav item classes. Keep existing structure; swap explicit Tailwind color classes to shadcn tokens. The Phase 1 Sidebar renders 5 items hardcoded — Phase 2 will replace this with RoleFilteredNav in plan 02-02, so keep the existing structure intact here; just migrate tokens:
```typescript
import { LayoutDashboard, FileText, Briefcase, ClipboardList, BarChart2 } from 'lucide-react';
import { NavLink } from 'react-router-dom';

const NAV_ITEMS = [
  { label: 'Dashboard', icon: LayoutDashboard, to: '/dashboard' },
  { label: 'Requests', icon: FileText, to: '/requests' },
  { label: 'Engagements', icon: Briefcase, to: '/engagements' },
  { label: 'Review Queue', icon: ClipboardList, to: '/review-queue' },
  { label: 'Reports', icon: BarChart2, to: '/reports' },
];

export function Sidebar() {
  return (
    <aside
      className="fixed left-0 top-0 h-full bg-secondary border-r border-border"
      style={{ width: 220, paddingTop: 64 }}
      aria-label="Main navigation"
    >
      <nav className="py-4">
        {NAV_ITEMS.map(({ label, icon: Icon, to }) => (
          <NavLink
            key={to}
            to={to}
            className={({ isActive }) =>
              `flex items-center gap-2 h-10 pl-4 pr-4 text-sm transition-colors cursor-pointer ${
                isActive
                  ? 'text-primary bg-muted border-l-4 border-primary pl-3'
                  : 'text-foreground hover:bg-muted border-l-4 border-transparent'
              }`
            }
          >
            {({ isActive }) => (
              <>
                <Icon
                  size={16}
                  className={isActive ? 'text-primary' : 'text-muted-foreground'}
                />
                <span>{label}</span>
              </>
            )}
          </NavLink>
        ))}
      </nav>
    </aside>
  );
}
```

**`frontend/src/components/layout/TopBar.tsx`** — update bg + border classes. Keep existing structure; swap to tokens:
```typescript
import { useAuthContext } from '../../context/AuthContext';
import { useNavigate } from 'react-router-dom';

export function TopBar() {
  const { user, logout } = useAuthContext();
  const navigate = useNavigate();

  const initials = user?.display_name
    .split(' ')
    .map((n) => n[0])
    .join('')
    .toUpperCase()
    .slice(0, 2) ?? '?';

  const handleLogout = async () => {
    await logout();
    navigate('/login');
  };

  return (
    <header
      className="fixed top-0 left-0 right-0 bg-background border-b border-border flex items-center px-6 z-10"
      style={{ height: 64 }}
    >
      {/* Logo */}
      <div className="font-semibold text-foreground text-base" style={{ width: 220 }}>
        EMS
      </div>

      {/* Search placeholder — replaced by GlobalSearchBar in plan 02-04 */}
      <div className="flex-1 mx-6">
        <div className="max-w-md mx-auto" id="global-search-container">
          <input
            type="text"
            placeholder="Search engagements, requests..."
            disabled
            className="w-full h-9 rounded-md border border-border px-3 text-sm text-muted-foreground bg-secondary cursor-not-allowed"
            aria-label="Global search (coming soon)"
          />
        </div>
      </div>

      {/* User menu */}
      <div className="flex items-center gap-3">
        <div className="flex items-center gap-2">
          <div
            className="w-8 h-8 rounded-full bg-primary text-primary-foreground flex items-center justify-center text-xs font-semibold"
            aria-hidden="true"
          >
            {initials}
          </div>
          <span className="text-sm text-foreground">{user?.display_name}</span>
        </div>
        <button
          onClick={handleLogout}
          className="text-sm text-muted-foreground hover:text-foreground px-3 py-1.5 rounded-md hover:bg-muted transition-colors"
        >
          Log Out
        </button>
      </div>
    </header>
  );
}
```

**`frontend/src/main.tsx`** — add `<Toaster />` inside `<BrowserRouter>` so toast() works globally:
```typescript
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import { Toaster } from './components/ui/toaster';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
    <Toaster />
  </React.StrictMode>
);
```

**Build verification:** After updating all files, run:
```bash
cd frontend && npm run build 2>&1 | tail -20
```
TypeScript must compile without errors. If import path errors occur with `@/`, verify `frontend/tsconfig.json` has `paths: { "@/*": ["./src/*"] }` and `frontend/vite.config.ts` has the resolve alias.
  </action>
  <verify>
grep -n 'bg-background\|bg-secondary' frontend/src/components/layout/AppShell.tsx && echo CONTRACT_OK
grep -n 'bg-secondary\|border-border\|text-primary' frontend/src/components/layout/Sidebar.tsx && echo CONTRACT_OK
grep -n 'Toaster' frontend/src/main.tsx && echo CONTRACT_OK
cd frontend && npm run build 2>&1 | tail -5 && echo "BUILD_OK"
  </verify>
  <done>
AppShell.tsx uses `bg-background`. Sidebar.tsx uses `bg-secondary`, `border-border`, `text-primary`, `text-muted-foreground`, `bg-muted`. TopBar.tsx uses `bg-background`, `border-border`, `bg-primary`, `text-primary-foreground`. `main.tsx` imports `Toaster` from `@/components/ui/toaster` and renders it inside `<React.StrictMode>`. `npm run build` exits 0 without TypeScript errors. Visual appearance is unchanged from Phase 1 (CSS variable values match Tailwind color class values exactly).
  </done>
</task>

</tasks>

<verification>
```bash
# 1. components.json exists with New York style
grep 'new-york' frontend/components.json && echo PASS

# 2. CSS variables aligned to Phase 1 tokens
grep 'primary: 221 83' frontend/src/index.css && echo PASS
grep 'secondary: 210 20' frontend/src/index.css && echo PASS

# 3. Tailwind uses CSS variables
grep 'hsl(var(--primary))' frontend/tailwind.config.ts && echo PASS

# 4. All 12 required components exist
ls frontend/src/components/ui/{button,input,badge,skeleton,toast,toaster,dialog,alert-dialog,table,checkbox,select,command,breadcrumb}.tsx 2>/dev/null && echo PASS

# 5. Toaster mounted
grep 'Toaster' frontend/src/main.tsx && echo PASS

# 6. Build succeeds
cd frontend && npm run build 2>&1 | grep -E 'error|Error' | grep -v '//\|#' && echo "HAS_ERRORS" || echo "BUILD_CLEAN"
```
</verification>

<success_criteria>
- `frontend/components.json` exists with `"style": "new-york"` and `"cssVariables": true`
- `frontend/src/index.css` defines `--primary: 221 83% 53%` matching blue-600 from Phase 1
- `frontend/tailwind.config.ts` maps `primary: 'hsl(var(--primary))'` and other CSS variable token colors
- All 12 shadcn components exist in `frontend/src/components/ui/`: button, input, badge, skeleton, toast, toaster, use-toast, dialog, alert-dialog, table, checkbox, select, command, breadcrumb
- Phase 1 components (AppShell, Sidebar, TopBar) compile without errors using shadcn token classes
- `Toaster` is mounted in `main.tsx` so `useToast()` calls work globally
- `npm run build` exits 0 with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-application-shell/02-01-SUMMARY.md`
</output>
